# ShadowTrace 发布前深度 Bug 探查报告

| 项 | 值 |
|---|---|
| 基准分支 / commit | `main` @ `738c47849a2eca981e437feeed5eaa8f95986e4e` |
| 探查日期 | 2026-08-11 |
| 立场 | fail-closed；无证据不臆测「已实现」 |
| 交付范围 | **仅问题与证据，不改代码** |
| 探查方式 | 静态（六维 + 多 Agent 交叉）+ 动态（本地门禁 / pytest）；Docker daemon / Postgres / Redis **不可用** |

## 执行摘要

**结论：需改后合 / 当前 main 在本机已复现多项 CI 必红缺陷，不建议按现状发布。**

动态证据表明：`contract-drift`、`mypy`、`ruff` 以及至少 1 条错误分类单测在本地失败；这些与 CI `contract-drift` / `backend-lint` / `backend-test` 门禁直接对应。P0 Mock 闭环语义（REQUIRED writeback gate、side-effect convergence、VerifyAgent 两阶段、DispositionCommand 白名单）在静态上大体成立，主要风险转为：**CI 绿假象被破坏、运维/配置旁路、BackgroundTasks 默认丢任务、契约/前端漂移、以及本环境无法跑通 Docker 全链路导致部分结论 SUSPECTED。**

| 级别 | 数量（去重后） |
|------|----------------|
| P0 Blocker（CI 必红 / 发布阻断） | 4 |
| P1 Should-Fix | 14 |
| P2 Nit / 运维注意 | 6 |
| SUSPECTED（缺 Docker/DB 动态证） | 5 |

---

## 1. 元信息与探查范围

### 1.1 近期 main 上下文

- 已合并 ISSUE-301（动态评测 matrix / strict CLOSED）与 ISSUE-302（CLOSED side-effect convergence）
- `git log main --oneline -15` 以 302/301/300/299/298 修复为主

### 1.2 P0 主链路文件图（已锁定）

```
Ingestion → EventService → investigate fork
  ├─ graph: SuperAgent / workflow_graph
  │    triage→planner→evidence→fp→[rag?]→graph→risk→response→approval→execute→verify→report→close
  └─ analysis_only: AnalysisOnlyPipeline（HTTP）；auto-investigate 仍走 SuperAgent
StateMachine + writeback_close_gate + side_effect_convergence
ActionExecution → DispositionSync → VerifyAgent → writeback_recovery
close/report APIs（含 force_local_close）
```

### 1.3 环境限制

| 依赖 | 状态 |
|------|------|
| Docker daemon | **未运行**（`docker.sock` 不可用） |
| Postgres `:5432` | Connection refused |
| Redis `:6379` | Connection refused |
| `GET /api/v1/health` | 未起服务 |
| `gh run list` | Forbidden（无权限读 Actions） |

因此：`make up` / `bootstrap` / `autonomous_mock_e2e` / production full-loop disposition **未能动态执行**，相关项标 SUSPECTED。

---

## 2. 维度 1 — 重大代码硬性 Blocker

### ID-BLK-001 | P0 | OpenAPI 契约漂移（ISSUE-302 字段未导出）

- **位置**：`backend/app/api/v1/schemas.py:244-259,301-316` vs `contracts/openapi/openapi.json`（`EventDetailResponse` / `EventCloseResponse` **无** `background_side_effects_pending` / `outstanding_side_effect_count` / `gate_applicable_outstanding_count`）
- **现象**：`scripts/check_contract_drift.py` 报 `[content_mismatch] openapi/openapi.json`；`tests/test_contracts/test_drift.py` 失败（`assert 1 == 0`）
- **根因假设**：ISSUE-302 合并后未执行 `make update-contracts`
- **证据**：动态命令输出见附录 A；手工解析 OpenAPI schema → `NO_SIDE_EFFECT_FIELDS`
- **修复建议**：`make update-contracts` 并提交；CI `contract-drift` 会强制

### ID-BLK-002 | P0 | mypy 类型错误（CI `backend-lint` 必红）

- **位置**：
  - `backend/app/services/side_effect_convergence.py:167-169`（`WritebackStatus` / `OutboxDeliveryStatus` 赋值混淆）
  - `backend/app/api/v1/events.py:865,1406,1561`（`**dict[str, int \| bool]` 展开进 `EventDetailResponse` / `EventCloseResponse`）
- **现象**：`mypy app` → **10 errors in 2 files**
- **根因假设**：ISSUE-302 降级计数用 `int \| bool` 字典展开，与 schema 字段类型不兼容；outbox 摘要循环变量类型复用错误
- **证据**：附录 A mypy 输出
- **修复建议**：为 side-effect 字段定义 TypedDict / 显式 kwargs；修正 `_summarize_outbox_fields` 变量类型

### ID-BLK-003 | P0 | ruff check / format 失败（CI `backend-lint` 必红）

- **位置**（抽样）：`app/core/metrics.py:434` E501；`app/services/state_machine_service.py` I001；测试文件 format drift（含 dynamic eval 相关测试）
- **现象**：`ruff check` **6 errors**；`ruff format --check` **4 files would be reformatted**
- **证据**：附录 A
- **修复建议**：`ruff check --fix` + `ruff format`；勿把 format-only 当功能回归掩盖

### ID-BLK-004 | P0 | `test_llm_error_subclass_retry_rules` 与实现不一致

- **位置**：
  - 实现：`backend/app/core/llm/base.py:74-76` — `LLMTimeoutError.default_retryable = False`（ISSUE-299）
  - 测试：`backend/tests/test_core/test_errors.py:299-300` — 仍断言 `is_retryable(LLMTimeoutError(...)) is True`
- **现象**：动态跑测 **FAILED**；`is_retryable` 实测 `False`
- **根因假设**：ISSUE-299 改语义后测试未同步 → 「测试绿」假设被打破；若 CI 全量跑该文件则红
- **证据**：本地 pytest 失败 + 运行时 print
- **修复建议**：更新测试与文档为「timeout 不可盲目重试」；确认 ReportAgent / BaseLLMClient 路径一致

### ID-BLK-005 | P1（设计旁路，非语义 bug）| `force_close` / `force_local_close` 跳过全部 CLOSED gate

- **位置**：`state_machine_service.py:523-556`；`events.py:1388-1407`
- **现象**：admin `force_local_close=true` 可在有 RUNNING job / READY outbox 时 CLOSED，并 `external_unsynced=True`
- **根因假设**：有意逃生舱；API 层有 `ROLE_ADMIN` 校验，**服务层无角色再校验**
- **证据**：静态代码；既有 `test_force_close_bypasses_side_effect_gate`（需 DB，本环境 SUSPECTED 复跑）
- **修复建议**：保持旁路但收紧 admin 发放；服务层 RBAC；审计告警必达

### ID-BLK-006 | P1 | `disposition_is_mock` 默认 True + mock 收据 simulated 无条件过 gate

- **位置**：`workflow.py:595`、`914-1040`（尤其 `1026-1040`）
- **现象**：Mock 模式接受 `simulated=True`；仅 `disposition_is_mock=False` 时拒绝 simulated 收据
- **影响**：staging 误配（live 模式却走 mock 收据 / 或 context 未由 SM 注入）可能假闭环或永久关不了
- **修复建议**：禁止非 SM 路径自建默认 mock context；启动时校验 `DISPOSITION_MODE` 与 gate 投影一致

---

## 3. 维度 2 — 功能被绕过 / Dead Code / No-op

### ID-BYP-001 | `memory_agent` / `tool_agent` 可被 Planner 选中但 graph 无节点

- **预期能力**：12 Agent 注册表含 Memory/Tool；Planner 合法名集合含二者
- **实际行为**：`workflow_graph` 无对应 node；`SuperAgent._execute_single_step` 未知 agent → 仅 warning
- **绕过点**：`agent_io.py` registry + `planner_agent.py:_VALID_AGENT_NAMES` vs `workflow_graph.py` 注册列表；Memory 仅 post-graph hook
- **影响范围**：LLM 计划若分配 memory/tool → **静默 no-op**
- **证据**：静态交叉；`workflow_graph.py` 无 Storyline/Memory node

### ID-BYP-002 | 默认 `TASK_MODE=background` → 重启丢调查任务

- **预期能力**：P0 可恢复执行（Redis 硬依赖 + 持久任务）
- **实际行为**：compose / config 默认 BackgroundTasks；进程重启 in-flight 丢失
- **绕过点**：`config.py:351`；`docker-compose.yml:122`；`events.py` `_schedule_investigation`
- **影响范围**：`make up` + `bootstrap` 默认路径；生产 fail-closed 禁止 background，但 demo 默认踩坑
- **证据**：静态 + deployment.md 自述「进程重启丢失」

### ID-BYP-003 | OutputGuard 缺失时 fail-open

- **位置**：`agents/base.py:270-271` — `output_guard is None` 直接返回 raw
- **影响**：非 DI 构造路径跳过结构化输出守卫
- **建议**：production 对副作用 Agent 强制要求 guard

### ID-BYP-004 | ConvergenceGuard 不可用时 replan 继续

- **位置**：`replan_handler.py:385-406`
- **影响**：失去步数/LLM 上限约束，仅 degraded flag
- **建议**：production fail-closed halt

### ID-BYP-005 | Trajectory API 将 DB 故障伪装为 `insufficient_trace`

- **位置**：`api/v1/trajectory.py:57-65`
- **影响**：运维无法区分「无轨迹」与「后端挂了」
- **建议**：基础设施错误返回 503

### ID-BYP-006 | OutputQualityEvaluator 异常 → 自动 PASS 0.75

- **位置**：`output_quality_evaluator.py:124-140`
- **影响**：质量门永不因评估器故障阻断主链路（业务 no-op 风险）
- **建议**：区分「可降级」与「必须阻断」路径；至少 sticky degraded

### ID-BYP-007 | AnalysisOnly vs SuperAgent 双路径行为不一致

- **位置**：`analysis_only_pipeline.py` vs `workflow_graph`；`deployment.md:230`
- **实际行为**：HTTP `ORCHESTRATION_MODE=analysis_only` 走 AnalysisOnly；**auto-investigate 仍走 SuperAgent**
- **影响**：同事件不同触发器得到不同闭环深度
- **证据**：文档已明示；属产品陷阱，非静默 bug

---

## 4. 维度 3 — 契约漂移与测试假象

### ID-CTR-001 | OpenAPI 与 schemas 漂移（同 ID-BLK-001）

见维度 1。

### ID-CTR-002 | `LLMTimeoutError` 测试与实现语义冲突（同 ID-BLK-004）

见维度 1。

### ID-CTR-003 | 默认 CI pytest 排除 `autonomous_mock_e2e` / `adversarial_audit`

- **契约项**：ISSUE-110 Celery 全环、对抗闭环
- **实现**：`pyproject.toml` `addopts` 排除 markers；`backend-closure-gates-mock`（显式 `LLM_MODE=mock`）另跑 adversarial + production disposition — **绿仅证明 Mock 管道，非 Live 研判/遏制覆盖**（ISSUE-350）
- **测试覆盖**：PR `backend-test` **不跑** worker E2E
- **风险**：Celery 重投递 / SIGKILL 回归仅靠 nightly/手动
- **证据**：`pyproject.toml` markers；CI workflow 分 job

### ID-CTR-004 | Orchestration 大量 StubAgent，真 Verify/写回另测

- **位置**：`tests/test_orchestration/test_workflow_graph.py` StubAgent
- **风险**：「图绿」≠「VerifyAgent + DispositionSync + close gate 绿」
- **缓解**：`backend-closure-gates-mock` 存在且 job 名/分数卡页眉标明 Mock；Live 对抗不阻塞 PR — 本环境未跑通 → **SUSPECTED 动态**

### ID-CTR-005 | 前端 `InvestigateResponse` 缺 `task_id` / `intent_id`

- **位置**：`frontend/src/services/eventApi.ts:86-91` vs `schemas.py` InvestigateResponse
- **风险**：Celery 模式 UI 无法展示/轮询 durable task
- **证据**：静态类型对比

### ID-CTR-006 | Playwright `frontend-e2e` 仅 `workflow_dispatch`

- **位置**：`.github/workflows/ci.yml` frontend-e2e 触发条件
- **风险**：UI 回归不挡 PR

### ID-CTR-007 | 仓库无 `xfail`，但大量 `skipif` 依赖外部服务

- **现象**：本机无 PG/Redis 时 integration 大面积 skip/ERROR
- **风险**：开发者本地「绿」可能只是「跳过」
- **证据**：grep `skipif` / `pytest.skip` 命中多处 fixtures

---

## 5. 维度 4 — 安全、权限与数据泄露

### ID-SEC-001 | P0 | `APP_ENV` 默认非 production → 生产 fail-closed 整组不生效

- **威胁**：漏设 `APP_ENV=production` 时仍可用 `DEV_AUTH_TOKENS`、mock/simulation、background tasks
- **位置**：`config.py:36`、`541-577`；`auth.py:151-153`
- **利用条件**：生产清单未强制 `APP_ENV=production`
- **严重级别**：P0（配置）
- **证据**：`production_fail_closed_violations` 仅 `is_production()` 时执行

### ID-SEC-002 | P1 | Compose `bootstrap-token` 含 **admin** 全角色

- **位置**：`infra/docker-compose.yml:133`
- **利用条件**：`APP_ENV` 非 production + 令牌泄露 → `force_local_close` 等特权 API
- **严重级别**：P1（demo 可接受，发布清单必须隔离）

### ID-SEC-003 | P1 | OTEL httpx instrumentation 可能导出 Authorization

- **位置**：`telemetry.py` `HTTPXClientInstrumentor`；`disposition/http_adapter.py` `_auth_headers`
- **利用条件**：`OTEL_ENABLED=true` + OTLP 后端不可信
- **严重级别**：P1
- **证据**：日志有 RedactingFormatter；span 侧未见等价脱敏钩子

### ID-SEC-004 | P1 | `COMPENSATION_RECORD` 不在交付期 approval 再校验集合

- **位置**：`disposition_sync_service.py:158-164`、`1231+`
- **利用条件**：补偿 outbox 入队后 rollback action 被 supersede，仍可能投递
- **严重级别**：P1
- **建议**：将 `COMPENSATION_RECORD` 纳入 `_DELIVERY_APPROVAL_RECHECK_INTENTS`

### ID-SEC-005 | P1 | 特权操作仅 API 层 RBAC，服务层信任调用方

- **位置**：`force_close`、`approval_engine._decide`、`resolve_unknown`、`resolve_writeback`
- **影响**：未来 Celery/脚本直调服务可绕过角色
- **严重级别**：P1（防御纵深）

### ID-SEC-006 | P2 | `ALLOW_LIVE_SIDE_EFFECTS=true` **阻断**副作用（命名陷阱）

- **位置**：`writeback_side_effect_fence.py:12-26`
- **影响**：运维启用开关期望「允许 live」实际得 ValidationError
- **证据**：单测 `test_writeback_side_effect_fence.py` 本地 **通过**；文档需极度醒目

### 已核实相对安全（无新 Blocker）

- `DispositionCommand` `extra="forbid"` + OutboundDispositionGuard 分析字段扫描
- `force_local_close` API 要求 admin
- approve/reject API `require_roles(ROLE_APPROVER)`
- 生产路径拒绝 `DEV_AUTH_TOKENS`（当且仅当 APP_ENV=production）

---

## 6. 维度 5 — 可靠性与竞态

### ID-REL-001 | P1 | POST `/investigate` 租约冲突吞成 HTTP 202

- **场景**：lease 被占用时仍返回 Accepted
- **位置**：`events.py:1309-1316`
- **失败模式**：intent 落库 pending，调度延后；客户端以为已启动
- **证据**：注释明确「不得 409」；对比 SuperAgent 自身会 409

### ID-REL-002 | P1 | Checkpoint memory fallback → 重启丢图状态

- **位置**：`orchestration/checkpointer.py` `_enable_memory_fallback`
- **失败模式**：Redis 抖动后落内存；worker 重启无法 resume
- **证据**：日志文案明示不可恢复

### ID-REL-003 | P1 | QUEUED job reclaim → Action `UNKNOWN` 人工堵死

- **位置**：`action_execution_service.py:456-472`
- **失败模式**：崩溃后幂等附着 in-flight job → UNKNOWN → close/verify 等 resolve-unknown
- **证据**：既有单测描述该行为（需 DB 复跑 → 部分 SUSPECTED）

### ID-REL-004 | P1 | Outbox lease 过期 → PAUSED/UNKNOWN，无 lookup 则关不了

- **位置**：`disposition_sync_service.py` lease expiry / UNKNOWN pause
- **失败模式**：严格防双投导致人工介入；正确但易在 demo 中表现为「卡死」
- **证据**：`test_expired_leased_must_pause_not_waiting_retry` 存在

### ID-REL-005 | P2 | E2E `poll_until` 固定超时易 flaky

- **位置**：`tests/integration/autonomous_e2e/helpers.py`
- **证据**：静态；本环境未跑 E2E

### ID-REL-006 | P2 | Side-effect / close reconcile 与 row-lock TOCTOU（SUSPECTED）

- **场景**：reconcile 在 `FOR UPDATE` 外；并发新 job 可能被误评估
- **缺证**：需并发集成测试 + 运行时日志

---

## 7. 维度 6 — 可维护性与答辩 / Demo 风险

### ID-DEMO-001 | P1 | Bootstrap smoke 只数事件 ≥3，不验终态

- **位置**：`scripts/smoke_bootstrap.sh`；`bootstrap.sh` 触发 investigate 后不等待 CLOSED
- **答辩风险**：三场景「冒烟通过」但实际卡在 waiting_approval/failed
- **复现步骤（需 Docker）**：`make up` → `make bootstrap` → 看 smoke 绿但事件状态非终态 — **本环境 SUSPECTED**

### ID-DEMO-002 | P1 | 文档「一键启动」导向易失任务的 background 栈

- **位置**：`docs/deployment.md:17-33` vs `:68-72` `up-demo`
- **复现**：按首页路径 `make up && make bootstrap` 而非 `up-demo`
- **建议**：一键路径改为 `up-demo` / `bootstrap-demo`

### ID-DEMO-003 | P1 | 本机无法拉起栈 → 答辩全链路未实测

- **证据**：Docker daemon 未运行；PG/Redis down
- **标记**：**SUSPECTED** — 需在有 Docker 的机器复跑：
  1. `make up-demo && curl /api/v1/health`
  2. `make bootstrap-demo` + 脚本化审批
  3. `make eval-full-loop` 或 matrix 单场景 `--require-closed`
  4. `pytest` integration/autonomous_e2e + `backend-closure-gates-mock`（Mock 管道，非 Live 研判证明）

### ID-DEMO-004 | P2 | 规划文档 ISSUE-296 Celery soft-timeout / graph-resume 热循环

- **位置**：`.planning/2026-08-10-dynamic-eval-audit/gh-issue-296.md`
- **风险**：真 LLM + Celery 下 demo/eval 可能打满 worker
- **状态**：文档已知；是否已在 main 修复 → **SUSPECTED**（需对照修复 commit / 动态复现）

### ID-DEMO-005 | P2 | CI Actions 状态本环境不可读

- **证据**：`gh run list` Forbidden
- **建议**：有权限账号核对 main 最近 5 次 workflow 是否已红（与本机 BLK-001~004 对应）

### ID-DEMO-006 | P2 | 本地 `make test` 仅跑 health，易造成「测试绿」错觉

- **位置**：`Makefile:206-207` — `pytest tests/test_infra/test_health.py`
- **答辩风险**：开发者执行 `make test` 不等于 CI `backend-test` / `backend-closure-gates-mock`；后者绿 **不得** 对外说成自主遏制覆盖（ISSUE-350）
- **关联**：`make ci-lint` 含 migration revision 检查，但**不含** `check-contract-drift`（契约门禁仅在 CI `contract-drift` job / `make check-contract-drift`）

### ID-CTR-008 | P2 | `backend-evaluation` 部分步骤为 report-only baseline

- **位置**：`.github/workflows/ci.yml` `backend-evaluation`（security/knowledge、agentic/coordination 标为 report-only；detection 为 required）
- **风险**：结构漂移可能不挡合并，取决于 `required_gate`
- **证据**：[CI/测试缺口探查](19c435b5-39b4-4584-8e2c-3abb1f839c48)

### 规划文档滞后提示（交叉验证）

[CI/测试缺口探查](19c435b5-39b4-4584-8e2c-3abb1f839c48) 引用的 `.planning/2026-08-10-dynamic-eval-audit/` 状态需对照当前 main，避免误报「未修」：

| 规划文档 | 规划当时状态 | 本审计对照 main@738c478 |
|----------|--------------|-------------------------|
| ISSUE-295 EventDetail unwrap | P0 | 已有 `unwrap_event_detail_payload` + gold-path 测试 |
| ISSUE-296 graph resume 热循环 | P0 | 仍作 DEMO/REL **SUSPECTED**（缺真 LLM Celery 复现） |
| ISSUE-298 Socket.IO health | P1 open | **代码侧已有** `health.py` → `socketio` 组件（规划文档滞后） |
| ISSUE-299 ReportAgent timeout | P1 open | 实现已 `LLMTimeoutError.default_retryable=False`；残留是**测试未对齐**（ID-BLK-004） |
| ISSUE-300 outbox not_found | P1 open | git 历史已有 merge #920；本环境未动态复验 → **SUSPECTED** |
| ISSUE-301/302 | P1 | 已合入 main；本报告 BLK-001~003 反映合入后契约/类型债务 |

---

## 8. 全链路测试矩阵

| 命令 / 套件 | 结果 | 说明 |
|-------------|------|------|
| `ruff check app tests` | **FAIL**（6） | ID-BLK-003 |
| `ruff format --check` | **FAIL**（4 files） | ID-BLK-003 |
| `mypy app` | **FAIL**（10） | ID-BLK-002 |
| `check_contract_drift.py` | **FAIL** | ID-BLK-001 |
| `pytest tests/test_contracts/test_drift.py` | **2 failed / 7 passed** | 同上 |
| `pytest test_errors`（含 LLM timeout） | **1 failed** | ID-BLK-004 |
| `pytest test_writeback_side_effect_fence` + `test_report_quality` | **23 passed** | 栅栏语义确认 |
| `pytest test_agents/test_verify_agent.py` | **128 passed** | Verify 单测 OK（非 DB 全环） |
| `pytest test_infra/test_dynamic_eval_*` | **69 passed** | ISSUE-301 脚本单测 |
| `pytest test_side_effect_convergence -k "not integration"` | 单元通过；integration **未跑** | 缺 PG |
| production_full_loop / adversarial / autonomous_e2e | **未执行** | 缺 Docker/PG/Redis |
| `make up` / health / bootstrap | **未执行** | Docker daemon down |
| `gh run list --branch main` | **Forbidden** | 无 Actions 读权限 |

---

## 9. 优先修复队列（发布前）

| 优先级 | ID | 动作 |
|--------|-----|------|
| 1 | ID-BLK-001 | `make update-contracts` 提交 OpenAPI（含 ISSUE-302 字段） |
| 2 | ID-BLK-002 | 修 mypy（side_effect_convergence + events kwargs） |
| 3 | ID-BLK-003 | ruff check/format 清零 |
| 4 | ID-BLK-004 | 对齐 `LLMTimeoutError` 测试与 ISSUE-299 语义 |
| 5 | ID-BYP-002 / ID-DEMO-002 | 默认演示路径改为 celery worker（`up-demo`） |
| 6 | ID-SEC-004 | `COMPENSATION_RECORD` 纳入交付期 approval 再校验 |
| 7 | ID-SEC-003 | OTEL httpx header 脱敏后再开生产 tracing |
| 8 | ID-BYP-001 | Planner 禁止未接线 agent，或 fail-closed |
| 9 | ID-CTR-005 | 前端 InvestigateResponse 对齐 task_id/intent_id |
| 10 | ID-DEMO-001 | smoke 断言场景终态 / analysis_only_complete |

---

## 10. SUSPECTED 待补证据清单

1. **Docker 全栈**：health、bootstrap 三场景终态、脚本审批、report 可读
2. **CI Mock closure-gates**：`test_production_full_loop_disposition` + adversarial 在干净 PG/Redis 上的 junit（`LLM_MODE=mock`；Live 对抗 nightly/手工）
3. **ISSUE-302 API**：`closed_side_effects_pending` 409、GET detail counts（integration）
4. **并发 close TOCTOU**（ID-REL-006）
5. **ISSUE-296** 是否仍可在真 LLM Celery 下复现 resume 热循环
6. **GitHub Actions main 近 5 次运行状态**（本机 gh Forbidden）

---

## 附录 A — 关键动态命令摘要

```text
# 环境
DOCKER: daemon not running
PG 5432 / REDIS 6379: Connection refused
HEAD: 738c47849a2eca981e437feeed5eaa8f95986e4e

# contract-drift
Contract drift detected:
  - [content_mismatch] openapi/openapi.json (JSON content differs)

# mypy（节选）
side_effect_convergence.py:167-169 assignment / comparison-overlap
events.py:865,1406,1561 EventDetailResponse/EventCloseResponse **dict 类型不兼容
Found 10 errors in 2 files

# ruff
Found 6 errors; 4 files would be reformatted

# pytest
test_contracts/test_drift.py: 2 failed, 7 passed
test_core/test_errors.py::test_llm_error_subclass_retry_rules FAILED
  is_retryable(LLMTimeoutError('timed out')) → False (expected True)
test_verify_agent.py: 128 passed
test_dynamic_eval_*: 69 passed
test_writeback_side_effect_fence + test_report_quality: 23 passed
```

---

## 附录 C — 2026-08-11 二次复核（遗漏 / 不实 / 冲突 / 与 main / 设计取舍）

复核基准仍为 `main@738c478`。运行时复跑（exit code）：`check_contract_drift=1`、`mypy=1`、`ruff=1`、`test_llm_error_subclass_retry_rules=1`；`LLMTimeoutError` `is_retryable=False`；OpenAPI 仍无 `background_side_effects_*` 字段。

### C.1 仍然成立（不实？→ **否，属实**）

| ID | 复核 |
|----|------|
| ID-BLK-001～004 | **属实**。本机再次失败；与 CI `contract-drift` / `backend-lint` / 默认 pytest 路径对齐。这是合入 ISSUE-302/299 后的**工程债**，不是臆测。 |
| ID-BYP-002 / DEMO-002 | **现象属实**：默认 `TASK_MODE=background`。见 C.3：属**故意设计**，应降级表述为「答辩/运维风险」而非「实现错误」。 |
| ID-SEC-006 `ALLOW_LIVE_*=true` 阻断 | **属实且故意**（P0 fail-closed 栅栏）；命名陷阱是 DX 问题，不是逻辑反了。 |
| ID-CTR-002/004/005、ID-REL-001 | **属实**（marker 排除 / Playwright 手动 / 前端缺 task_id / investigate 202）。 |

### C.2 定级或表述需修正（不实 / 过度）

| ID | 问题 | 修正 |
|----|------|------|
| **ID-BYP-001（Memory 不在 graph）** | **部分不实**。`ShadowTrace 工程实施拆解方案` ISSUE-080 明确 MemoryAgent 是 **结案后置 hook**（`_schedule_memory_after_close`），不是 P0 graph 节点。真正问题是 Planner `_VALID_AGENT_NAMES` 仍可把 `memory_agent`/`tool_agent` 写进计划导致静默 warning。 | 收窄为「Planner 可派未接线 agent」；删除「Memory 缺节点 = bypass bug」 |
| **ID-BLK-005 force_close** | **故意设计**（方案 ISSUE-037 / 状态机 §：唯一 admin 逃生舱 + `external_unsynced`）。报告已标设计旁路，但放在「Blocker 维度」易误读为必须删。 | 移出 BLK；保留为 SEC/运维「收紧 admin + 服务层 RBAC」Should-Fix |
| **ID-BLK-006 disposition_is_mock 默认 True** | **故意 + Mock P0 必需**：方案要求 Mock receipt `simulated=true` 且 gate 接受；SM 权威投影。默认 True 是 fail-open 给错误调用方的风险，但改默认 False 会破坏未注入 context 的测试/路径。 | 降为 P2「禁止非 SM 自建 TransitionContext」；勿改默认值当主修 |
| **ID-BYP-006 quality auto-PASS** | 代码注释明示 fail-safe 不挡主链路。 | 故意；保留 degraded 可观测性建议即可 |
| **ID-REL-001 investigate→202** | 测试与注释明示「intent 已落库不得 409」。 | 故意 API 语义；风险在调度失败时的 reconcile，非 HTTP 码本身 |
| **ID-REL-003/004 UNKNOWN/PAUSED** | fail-closed 防双投 / 未知投递。 | **好处 > 坏处**；属正确行为，demo 卡死是运维体验问题 |
| **规划文档 ISSUE-298 open** | 附录已纠正：health 已有 socketio。 | 勿再当未修缺陷 |

### C.3 相互冲突（报告内部）

1. **Mock 闭环 vs ID-BLK-006**：一边要求 Mock `simulated=true` 能 CLOSED，一边把「mock 默认过 gate」当假闭环风险。→ 正确表述是：**仅当 `disposition_is_mock` 与真实 `DISPOSITION_MODE` 不一致时才危险**；SM 注入正确则无冲突。
2. **默认 background vs「发布阻断」**：方案明文默认 background、生产禁 background。→ 不能既保留开发默认又把「存在 background 代码路径」当 Blocker；Blocker 应是「生产清单漏设」或「答辩用了 `make up` 而非 `up-demo`」。
3. **ID-BLK-004**：修法只能是**改测试跟 ISSUE-299**；若改回 timeout 可重试，会与 299 设计冲突。
4. **ID-SEC-004 服务层 RBAC vs force_close 方案**：方案写「认证在本服务完成」，当前实现角色只在 API 校验 → 与方案字面**略有偏差**，补服务层角色更贴方案，不冲突。

### C.4 修完后与当前 main 的冲突面

| 修复项 | 与 main 冲突风险 |
|--------|------------------|
| `make update-contracts`（BLK-001） | **低**：纯契约刷新；可能与未提交本地改动冲突，与业务逻辑无冲 |
| mypy/ruff（BLK-002/003） | **低**：类型与格式；注意 side_effect 字段 TypedDict 勿改变 API JSON 语义 |
| 对齐 LLM timeout 测试（BLK-004） | **低**：只改测试；勿改回 `default_retryable=True` |
| 改 `TASK_MODE` 默认→celery | **中**：违背方案「默认 background」；应改文档/答辩路径，或仅 compose demo profile |
| 改 `disposition_is_mock` 默认 False | **高**：易炸 mock 测试与未注入 context 的调用 |
| 删除/收紧 force_close | **高**：违背 ISSUE-037 逃生舱合同；只应加固 RBAC/审计 |
| Planner 剔除 memory/tool | **低～中**：需确认 disposition-only / LLM 计划是否依赖这些名字 |
| COMPENSATION 交付再校验（SEC-003） | **低～中**：可能挡住合法补偿投递时序，需补测 |
| investigate 改回 409 | **中**：与现有 API 测试/产品语义冲突，不建议 |

**结论**：优先修 BLK-001～004 **不会**与状态机/写回合同冲突；动「默认 background / mock gate / force_close / investigate 202」才会与方案打架。

### C.5 好处 ≥ 坏处的故意设计（应保留，报告应降噪）

| 行为 | 好处 | 坏处 | 建议 |
|------|------|------|------|
| force_close + external_unsynced | 写回不可用时仍可本地结案 | admin 误用假闭环 | 保留；收紧角色与审计 |
| Mock simulated 过 CLOSED | P0 无真 XDR 可验收 | 误配 live+mock | 保留；靠 APP_ENV/MODE 栅栏 |
| TASK_MODE=background 默认 | 本地无 Redis worker 也能试 | 重启丢任务 | 保留默认；答辩强制 up-demo |
| ALLOW_LIVE true→阻断 | P0 禁 live 副作用 | 命名反直觉 | 保留逻辑；改名/文档 |
| outbox PAUSED / action UNKNOWN | 防双投、未知侧效 | 人工介入 | 保留 |
| quality eval 失败 PASS | 评估器不拖死主链 | 质量门空转 | 保留；加强 degraded 可见性 |
| Memory 后置 hook | 结案不被记忆拖垮 | Planner 误派 | 保留 hook；收紧 Planner |

### C.6 可能遗漏（二次扫描仍建议跟踪，非全部升 P0）

1. **远程 CI 实况未取证**（gh Forbidden）— 本地必红强烈暗示 origin/main CI 红，但仍缺 Actions UI 截图。
2. **Docker 全环 / bootstrap 终态** — 仍 SUSPECTED。
3. **`make test` 仅 health** — 已记 DEMO-006；易造成本地假绿。
4. **服务层 RBAC 与方案字面差一截** — 见 C.3.4。
5. **Planner 可派未接线 agent** — BYP-001 收窄后仍值得修。
6. 未再发现新的「REQUIRED 未 CONFIRMED 却 CLOSED」主路径旁路（除故意 force_close / NOT_REQUIRED quick-close）。

### C.7 复核总判

- **遗漏**：有少量（CI 远端、Docker 全环、Planner 收窄表述）；无颠覆性新 P0 语义洞。
- **不实**：BYP-001 对 Memory「缺节点」过度解读；若干项应标「故意设计」而非 Bug。
- **冲突**：报告内 Mock gate / background / timeout 修法存在表述张力；按 C.3 消解。
- **与 main 冲突**：修契约/类型/测试安全；改默认值与逃生舱危险。
- **设计取舍**：force_close、mock simulated、background 默认、live 栅栏、UNKNOWN/PAUSED — **保留，好处大于坏处**。

**发布建议（复核后）**：仍「需先清 BLK-001～004 再谈发布」；P1 中刻意设计项改为「加固/文档」，不要当缺陷大删。
