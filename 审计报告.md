# ShadowTrace 发布前深度 Bug 探查报告

| 项 | 值 |
|---|---|
| 产品基准 | `origin/main` @ `d524a16f725b6ad43c7088e725b622cd4ca159b5`（含 ISSUE-328 / 350） |
| 探查日期 | 2026-08-16（含同日对 §9 FIX 清单的源码复核） |
| LLM | **Live `openai_compatible` / `glm-5.2`**（Ark chat-completions；密钥只在进程环境，**未写入仓库/本报告**） |
| 立场 | fail-closed；Mock 绿 ≠ 研判；Live PASS 仍要看理解/覆盖/认证口径 |
| 交付 | **只出问题与证据，不改产品代码** |

## 执行摘要

**结论：glm-5.2 对抗全闭环能 CLOSED，且 ISSUE-328 覆盖门补上了模型故意留下的 DB 主机。这还不是「高质量闭环」。** 上一版把 FIX-001–009 一律写成必须改，源码复核后 **不成立**：有的根因写错，有的是 ISSUE-032/212/309/328 有意合同（好处大于改掉），有的互相打架。仍要动的是 §9.1；故意留下的见 §9.2。

亲自跑了两条 **Agent 全链路**（不是 `make demo-full-loop` 金链路）：

1. AnalysisOnly → `REPORTING`（管线 **60.6s**，pytest 82s）**PASSED**
2. SuperAgent full_loop → **`CLOSED`**（图 **202.9s**，pytest 226s）**PASSED**；`llm_call_count=11`，`generated_by=llm`

上一轮同场景 Live（328 前）规则回退 **零 isolate**。本轮 glm 自己隔离了 WKS，但策略写明 **「SRV-DB-STG-02 remains online」**；`containment_quality_gate: entity_coverage_merge` 用 `reason=rule fallback` 补了 `isolate_host(SRV-DB-STG-02)`。覆盖门生效，**模型研判仍倾向不隔 DB**。

| 级别 | 本轮（复核后） |
|------|------|
| 仍应修 | **FIX-001**（CI 合同冲突，不是 unevaluable 计错）、**FIX-002**（10 并发租约丢失）、**FIX-003**（prompt/strategy 与门叙事对齐，勿拆 328）、**FIX-007**（#970 顺序敏感 ImportError，P2）、**FIX-008**（身份工具去重，P2） |
| 不按必须改执行 | **FIX-004**（ISSUE-032）、**FIX-005**（ISSUE-212）、**FIX-006**（ISSUE-309/347）、**FIX-009**（328/339 只封 dest，产品策略） |
| 已合入不重报未修 | ISSUE-328（#959 CLOSED）、350（job 名 `backend-closure-gates-mock`）、335–349、351–353 |
| 不要改 | 334 理解定义、312 CLOSED 不查 isolate、305 Memory 不进图、Mock `mock_simulated`；detection 两例 fail-closed 切片不要改成「检出威胁」 |
| SUSPECTED | 无 Docker daemon 未跑 compose；非 mock 写回 `status_queried` vs CLOSED 强证据门（本环 Mock 未踩） |

**密钥**：本次用你提供的 Ark key 调 glm。请视为已暴露，**尽快轮换**。本文件不含密钥。

---

## 1. 方法与环境

### 1.1 读过

`README.md`、`ShadowTrace 工程实施拆解方案.md`、`.github/workflows/ci.yml`；P0 链路 ingestion → EventService → SuperAgent/`workflow_graph` → Agents → StateMachine → ActionExecution → DispositionSync → VerifyAgent → close/report。

### 1.2 动态

本云 VM **无 Docker daemon**。Postgres 16+pgvector / Redis 7 本机安装。对抗 pytest：

```bash
LLM_MODE=openai_compatible LLM_PRIMARY_MODEL=glm-5.2 \
LLM_API_BASE_URL=https://ark.cn-beijing.volces.com/api/coding/v3 \
ALLOW_LIVE_SIDE_EFFECTS=false DISPOSITION_MODE=mock_xdr \
ADVERSARIAL_FULL_LOOP_TIMEOUT_S=600 \
uv run --frozen python -m pytest tests/adversarial/test_agent_adversarial_audit.py \
  -m adversarial_audit -v -s -o addopts=
# 以及 test_agent_adversarial_full_loop.py 同标记
```

产物：`backend/tests/adversarial/artifacts/latest_audit.json`、`latest_full_loop_audit.json`（gitignored）。

对照：Mock 同场景曾 ~5s / ~12s；本次 61s / 203s，可排除误跑 golden 瞬时路径。

### 1.3 CI / Issue

- `gh run list --branch main`：最新 `31917018845`（#1017 合入后）**failure**。lint / `backend-closure-gates-mock` **success**。evaluation + system-test **failure**；backend-test **cancelled**。
- OPEN 产品 bug：#970 循环导入、#882 P2 构建。328/350 已关。
- `ruff check` / `ruff format --check` 本机全绿。`backend/tests` **零 xfail**。

---

## 2. 研判质量主证据（glm-5.2）

场景 `adversarial_credential_db_staging_exfil`：糊标题、高噪声；GT：`svc-analytics-47` / `WKS-DATA-031` / `SRV-DB-STG-02` / VPN `198.51.100.44` / 域 `storage-sync-cdn.example`。诱饵 `BACKUP-SRV-01`。

### 2.1 分数卡

| 维 | AnalysisOnly Live | SuperAgent full_loop Live |
|----|-------------------|---------------------------|
| pytest | PASSED | PASSED |
| 耗时 | 管线 60.6s | 图 202.9s；审批 1 轮 / **12** 个动作 |
| 终态 | `REPORTING` | **`CLOSED`** |
| type / risk / verdict | `data_exfiltration` / **74** / `confirmed_threat` | 同，risk **75** |
| 对外 / 分诊 severity | high / **medium** | 同 |
| text_understanding | 实体 **0/3**，IOC **0/2** | 同 |
| source_projection | 3 实体 + VPN + 上传域 | 同 |
| echo_only | 与 source 相同（附录回声） | 同 |
| DNS / 采集 | completed；无 query_dns skip-failed | 同 |
| 遏制 | 分析路径无 response（coverage GAP 含账号/WKS/VPN/**DB**） | **WKS+DB 均 isolate**；disable + force_logout + revoke_token；block 域 + 两外网 IP；quarantine SQL；scan 两主机；**无 BACKUP** |
| 计划来源 | n/a | `generated_by=llm`；strategy 末尾 `; containment_quality_gate: entity_coverage_merge` |
| DB isolate 原因 | n/a | **`rule fallback`**（门补），非模型主动 |
| 写回认证 | n/a | `simulated=true`，label **`mock_simulated`** |
| 人类 verdict | PASS + GAP + understanding 0 | PASS + understanding 0 + writeback=simulated |
| 计分 | 5/5 | **7/7**；`disposition_targets_aligned` **且** `strict_aligned=true`；gaps `[]` |
| report_quality | `degraded_template`（不挡关单） | 同 |
| OutputQuality | triage **0.565 warn**；report 0.595 warn | triage 0.565 warn；risk 0.85 |

### 2.2 glm 实际计划 vs 覆盖门

模型 strategy（截断）：先工单/通知 → 封外网域和两个外网 IP → 撤 token / 强制下线 → 隔离 SQL 文件 → 扫描主机 → **再** disable 账号、isolate **WKS**。明确写：**「SRV-DB-STG-02 remains online pending investigation to avoid unnecessary disruption」**。

门在 [`apply_containment_quality_gate`](backend/app/agents/rules/response_plan_quality_gate.py) 444–505 做 **EntitySet 覆盖 merge**（不再「任意 CONTAINMENT_TOOLS 即 return」）。DATA_EXFIL HIGH 规则现含 `isolate_host`（[`default_response_rules.py:113-118`](backend/app/agents/rules/default_response_rules.py)）。结果：DB isolate 以 `rule fallback` 进计划。诱饵主机未进 EntitySet，产物无 `BACKUP`。

**研判含义：** 闭环覆盖靠门，不靠 glm 主动隔 DB。答辩不要说「模型自己隔离了失陷库」。`reason=rule fallback` 是门优先拷贝规则池候选，不是门没跑。叙事对齐见 FIX-003；不要为此拆 328。

### 2.3 其它研判观察

- 糊标题不含三实体名；理解 0 是 ISSUE-334 合同。summary/报告能点名实体 = **附录回声**，不是读糊标题。
- glm 封 `198.51.100.77`（egress dest）和域，合理；同时封 VPN **源** `198.51.100.44`，reason 写 *source*，不是 327 把源写成 dest。是否该封 VPN 出口是产品策略，不是代码 bug。
- `revoke_token` 为 L4，12 动作 blast radius 大于 Mock golden（golden 约 4–5 条）。Mock XDR 全 success，**不代表** live XDR 审批/变更窗口可承受。
- AnalysisOnly 不计处置覆盖进 pytest 硬失败，但人类行已旁注 GAP（含 DB）。

---

## 3. 维度 1 — 重大代码硬性 Blocker

### ID-BLK-001 | 已排除（本轮） | 假闭环 / 无 CONFIRMED 即 CLOSED

[`validate_closed_gate`](backend/app/models/workflow.py) 仍要求报告 + side-effect 收敛 + 终态 `EVENT_STATUS_UPDATE` CONFIRMED。非 mock 拒 simulated / 弱 evidence。本环 Mock 宽门，`certification_label=mock_simulated`。Verify 跑了（`verify_agent_ran=true`）。

### ID-BLK-002 | 已排除（本轮） | ISSUE-328 任意遏制即放行

328 已合入覆盖 merge。本 Live：`strict_aligned=true`，DB 被补隔离。**残留研判债**见 §2.2（模型不想隔 DB）——不再当「门过粗导致能 CLOSED 却零 isolate」。

### ID-BLK-003 | P0（CI 合同） | `backend-evaluation` detection `fail_closed`

- **位置**：CI run `31917018845`；detection 切片 `pass_rate=0.666…`，`gate_verdict=fail_closed`（前几个 agentic/security 切片 pass_rate=1.0）
- **触发**：main push 必跑 evaluation job（`required_gate: true`，`min_pass_rate: 1.0`，`max_error_count: 0`）
- **影响**：发布门禁红。Redis 已在（337）。
- **证据**：已下载 `detection_ci_run.json`。7 案：`pass=4` / `fail=0` / `unevaluable=1` / `error=2`。`unevaluable` **不进** pass_rate 分母（`pass_rate = 4/(4+0+2)`）。两例 ERROR 是 `threat_cold_start_insufficient_history`（缺 feature baseline）与 `threat_resource_budget_exceeded`（scan budget）。单测与 `baseline_artifact.json` **断言/钉死**同一 FAILED 快照。
- **修复**：拆「发布门数据集」与「fail-closed 演示切片」，或给预期 fail-closed 单独计 PASS 后重钉 baseline。**禁止**把 unevaluable 改成 pass、禁止让 cold-start 硬检出威胁、禁止 `required_gate: false` 掩盖混装数据集。详见 FIX-001。

### ID-BLK-004 | P0（CI） | system 并发冒烟租约丢失

- **位置**：`tests/system/test_concurrency_smoke.py::test_ten_concurrent_events_reach_terminal_state_without_cross_talk`
- **现象**：`InvestigationLeaseLostError: investigation lease lost during orchestration`（此前 run 还见过 `DeadlockDetectedError`）
- **影响**：`backend-system-test` 红；backend-test 被 cancel。
- **证据**：run `31917018845`。
- **修复**：锁顺序/死锁重试；租约丢失应单事件 fail-closed，勿打翻整组。不要把并发改成 1。详见 FIX-002。

### ID-BLK-005 | 已排除 | lint E501/format

本机与该 run `backend-lint` **success**。

### ID-BLK-006 | 已排除 | Verify `already_submitted` 跳过两阶段

本 Live 走到 CLOSED，verify traced。

---

## 4. 维度 2 — Bypass / Dead Code / No-op

### ID-BYP-001 | 覆盖门 vs 模型意图

- **预期**：高置信已识别主机应隔离。
- **实际**：glm 明确不隔 DB；**门补上了**。能力在，模型路径会被更强规则替代。
- **绕过点**：无（门挡住了漏隔离）。质量问题是「模型保守过度」。
- **证据**：strategy 原文 + `isolate_host SRV-DB-STG-02 reason=rule fallback`。

### ID-BYP-002 | Memory/Storyline 不在图

ISSUE-305 有意。本环 12 条 agent_trace，无 memory 节点。不要把 Memory 塞进图。

### ID-BYP-003 | Trajectory 空轨迹 | 可接受 UX

[`trajectory_analyzer.py:47-58`](backend/app/services/trajectory_analyzer.py) 真的空轨迹 → `insufficient_trace=True`。基础设施失败已改 503（341）。

### ID-BYP-004 | 默认 bootstrap 非 CLOSED

ISSUE-352：`bootstrap-demo` = 分析种子；`demo-full-loop` / `bootstrap-demo-full-loop` 才是 CLOSED。本环境无 Docker，未跑 compose。

### ID-BYP-005 | AnalysisOnly 无处置

设计如此。人类 PASS 旁注 coverage GAP。不要把 AnalysisOnly 绿说成闭环。

---

## 5. 维度 3 — 契约漂移与测试假象

### ID-CTR-001 | Mock CI ≠ Live 研判

- **契约**：job 现名 **`backend-closure-gates-mock`**（ISSUE-350），本 run **success**。
- **实现**：CI 仍默认 Mock LLM。
- **测试**：本 Live 全闭环 203s + 12 动作，与 Mock golden 计划形状不同。
- **风险**：仍可能口头把 mock job 绿说成 glm 能力。名称已降低风险。

### ID-CTR-002 | 计分不含理解维

5/5 与 0/3 并存（319/334/349）。旁注已有。风险：截图只留 PASS。

### ID-CTR-003 | DB gated | **已解（328）**

`must_response_targets_gated` 为空。本 Live `strict_aligned=true`。

### ID-CTR-004 | OpenAPI | 本 run `contract-drift` success

### ID-CTR-005 | skipif 绑 PG/Redis；零 xfail

无 Docker 切片会 skip。本次对抗有 PG/Redis 已跑。

### ID-CTR-006 | report_quality 不挡 CLOSED

图内 `degraded_template` 仍 7/7 PASS（ISSUE-348 合同：不把 HTTP 212 当 CLOSED 硬门）。ISSUE-212 规定 `generated_by=template` 永不为 `complete` 是诚实降级，**不是**「报告门空转」的必须改项。见 §9.2 FIX-005。

---

## 6. 维度 4 — 安全 / Fail-Open

### ID-SEC-001 | `DEV_AUTH_TOKENS` 生产拒绝 | fail-closed，非 bug

### ID-SEC-002 | 分析出站 | 未找到 DispositionCommand 拷 prompt/report 路径

### ID-SEC-003 | `raw_payload` 明文落库 | P1 SUSPECTED

未做带密码字段抓包。GET source-record 不回 raw。缺证据保持 SUSPECTED。

### ID-SEC-004 | `force_local_close` | P2 有意

服务层 + API admin RBAC（308）。跳过写回门，标 `external_unsynced`。

### ID-SEC-005 | Mock 冒充 live | 本环排除

`ALLOW_XDR_WRITEBACK=false`；认证 `mock_simulated`。产物里仍有字段名 `confirmation_evidence=readback_verified`（收据档位）——**对外必须用 certification_label**，勿念成已 live 验证（351）。

### ID-SEC-006 | 聊天中的 API 密钥 | P0 运维

密钥出现在对话里。轮换 Ark key。禁止把 key 写入 `.env` 后 commit。

---

## 7. 维度 5 — 可靠性 / 竞态

### ID-REL-001 | 10 并发调查租约丢失 | 同 BLK-004

### ID-REL-002 | degraded_flags | 353 后 bootstrap 走 factory；本 Live 未打 Redis 宕机

### ID-REL-003 | 本 Live 无 sunset shim、resume_attempts=0

`sunset_shims_used=[]`。图一次跑完。

### ID-REL-004 | glm 计划动作数大（12）+ L4

审批 12 条全过（测试脚本批准）。生产人工审批成本/误伤面上升。属可靠性/运营，不是状态机 bug。

---

## 8. 维度 6 — DX / Demo / 答辩

### ID-DEMO-001 | 无 Docker，未跑 `make up-demo && make demo-full-loop`

答辩不要声称本审计验证了 compose 金路径。对抗 harness 已覆盖 Agent 图 CLOSED。

### ID-DEMO-002 | main CI 非全绿

run `31917018845`：lint / mock closure-gates / contract / docker-build / regression / frontend-build 绿；**evaluation + system-test 红**。

### ID-DEMO-003 | 双 severity | 330 已披露

对外 high、分诊 medium。glm 分诊摘要仍按 medium 叙事。

### ID-DEMO-004 | 开放项

#970 循环导入（顺序敏感，API 常走路径可加载）；evaluation/system CI。328/350 已关。

### ID-DEMO-005 | 答辩一句

AnalysisOnly pytest 绿停在 REPORTING，理解 0。SuperAgent Live glm **能 CLOSED 且 WKS+DB 都隔离**，但 DB 隔离来自 **328 覆盖门**，模型原文要让库继续在线。写回是 **Mock simulated**。CI 评测红是 **fail-closed 切片与 required_gate 合同冲突**，不是 unevaluable 计错。高质量闭环修 §9.1，不要为刷绿去拆 032/212/309/328。

---

## 9. 高质量全链路闭环 — 源码复核后的清单

上一版把 FIX-001–009 都写成「必须改」。对照 `origin/main` @ `d524a16f` 源码、CI artifact `detection_ci_run.json`、以及 ISSUE-032/212/309/328/347/#970 合同后，按五问重判。

### 9.0 五问结论

| 问 | 结论 |
|----|------|
| 1. 是否有遗漏 | 有，但 **不升级为必须改**：非 mock `status_queried` vs CLOSED 强证据门（`workflow.py` 注释已写明 Verify 只降 `adapter_acknowledged`，本环 Mock 未踩）；`backend-test` 被 cancel 是 system-test 红的 CI 图后果，不是第三条产品 bug；compose 金路径无 Docker 仍 SUSPECTED。ReportAgent 已注入 `llm_client`，日志未见 fallback 字样，不能把 degraded 单写成「LLM 必失败」。 |
| 2. 是否不实 | **FIX-001 根因不实**（unevaluable 未进 pass_rate 分母）。**FIX-005「模板永不能 complete 所以必须改评分器」不实**——那是 ISSUE-212 硬合同。**FIX-007「HTTP investigate 会 500」过述**——`import app.orchestration` / `from app.api.v1.deps import get_workflow_runtime` 成功；只有先 `import analysis_only_pipeline` 才炸。**FIX-004 因果过强**：user payload 已有 `risk_severity=high`，glm 仍保守，不能单怪分诊没扫 `normalized.src_ip`。 |
| 3. 是否相互冲突 | **是**。FIX-003「prompt 每个 host 都 isolate」vs 328「只覆盖 EntitySet、永不隔诱饵」vs FIX-008「少动作」。FIX-004 vs ISSUE-032/330。FIX-005 vs ISSUE-212。FIX-006 vs ISSUE-309。FIX-001「让那两例威胁切片过 pass_rate」vs 单测/baseline 断言 FAILED。 |
| 4. 修完是否与当前 main 冲突 | 本报告仍只改文档。若按上一版修法改产品：**会打 main**。FIX-001 让 cold-start/budget 检出威胁会红 `test_cold_start_insufficient_history_fail_closed` / `test_resource_failure_fail_closed` / 钉死的 `baseline_artifact.json`。FIX-004 会红 ISSUE-032 分诊测。FIX-005 让 template→complete 会红 ISSUE-212。FIX-006 全局 blocking=true 回潮 309。FIX-003 对库存盲扩 isolate 回潮 328 诱饵合同。 |
| 5. 是否故意设计且好处>坏处 | **FIX-004/005/006/009 以及 328 覆盖门、detection 两例 fail-closed、租约丢失 fail-closed：是。** 保守 Response prompt + 硬覆盖门（328）好处是永不漏 EntitySet 主机、永不隔诱饵；坏处是 strategy 可能与已执行 isolate 打架——只值得做 **叙事对齐**，不值得拆门。 |

「高质量闭环」复核后的验收（改掉第 3、4 条，避免与 328/032/212 对打）：

1. main CI：lint / system / evaluation / `backend-closure-gates-mock` **合同自洽后**全绿（禁止 skip/xfail/关 `required_gate` 刷绿；evaluation 绿必须来自「发布门数据集」或「预期 fail-closed 计 PASS」，不是让 cold-start 假检出）。
2. Live glm 对抗 full_loop：`CLOSED` + WKS **与** DB 均 `isolate_host` + 账号 disable + 外网 **dest** block；诱饵不隔离。覆盖可由 **328 门**保证。
3. strategy / 审批叙事 **不得**写「DB remains online」若门将补 isolate；prompt 要求隔离的是 **EntitySet hosts**，不是资产库存里每一台。门仍是安全网，不要求「模型单独完成隔离才算闭环」。
4. 报告诚实：template 就是 `degraded_template`（212）；不要为答辩把 template 改成 complete。分诊 medium + risk high 是 032/330 双列，不要为了让 glm 更狠而改分诊枚举。
5. 10 并发调查不因租约/死锁整组红；**单事件租约丢失仍 fail-closed**。
6. `import app.services.analysis_only_pipeline` 与先 import graph 两条顺序都不 `ImportError`（#970）。不把「API 常走路径能加载」说成环已消失。
7. 写回对外只用 `mock_simulated`；理解 0 继续旁注，**不要**把 echo 算读懂糊标题。

### 9.1 仍应修

### FIX-001 | P0 | CI 合同冲突：detection 数据集故意 ERROR，required_gate 却要 pass_rate=1.0

- **位置**：manifest [`data/evaluation/detection_shadow_v1/threshold_manifest.json`](data/evaluation/detection_shadow_v1/threshold_manifest.json)；聚合 [`backend/app/evaluation/detection/runner.py`](backend/app/evaluation/detection/runner.py) `evaluable = pass+fail+error`，**unevaluable 不进分母**；评分 [`ThreatDetectionScorer`](backend/app/evaluation/detection/scorers/slice_scorers.py) 对 `runtime_errors` 返回 ERROR；CI [`scripts.run_detection_evaluation`](backend/scripts/run_detection_evaluation.py) 在 `gate_verdict=fail_closed` 或 `artifact.status != completed` 时退出 1。
- **触发**：CI `backend-evaluation` run `31917018845`。
- **已核实 artifact**（`detection-evaluation-run-artifact` / `detection_ci_run.json`）：

  | case_id | status | 要点 |
  |---------|--------|------|
  | 3× benign + `threat_event_match` | completed | 计入 pass |
  | `unevaluable_partial_telemetry` | unevaluable | `unevaluable_coverage=1.0`，**不进** pass_rate |
  | `threat_cold_start_insufficient_history` | failed | `threat_detection` ERROR：`missing detection feature baseline for snapshot entity` |
  | `threat_resource_budget_exceeded` | failed | `threat_detection` ERROR：`observation scan cost limit exceeded`；`resource_budget` FAIL |

  故 `pass_rate=4/6≈0.667`，`error_count=2`。`baseline_artifact.json` **同一 FAILED 快照**。单测 [`test_detection_shadow_v1_full_dataset`](backend/tests/evaluation/test_detection_runner.py)、`test_cold_start_insufficient_history_fail_closed`、`test_resource_failure_fail_closed` **断言**这两例必须 FAILED/ERROR。
- **影响**：发布 job 结构性地红。这是 **required_gate 混装了 fail-closed 演示切片**，不是 unevaluable 被当成 fail，也不是漏检。
- **工业修复**：把 4 个应过 + 1 个 unevaluable 做成发布门数据集（`required_gate` 仍 true）；两例 fail-closed 切片留在 observe/`--allow-gate-fail` job，或给它们 `expected_outcome=fail_closed` 在「按设计 ERROR」时计 PASS，然后重钉 baseline。CLI 已有 `--allow-gate-fail`，**不要**用它冒充 required 绿。
- **禁止**：`required_gate: false` 直接罩住混装集；把 `min_pass_rate` 降到 0.66；改评分器让 unevaluable 变 pass；让 cold-start/超预算切片 **检出威胁**（那会打 main 单测与 fail-closed 语义）。
- **验收**：required detection job `gate_verdict=pass` 且 `required_gate` 仍 true；那两例 fail-closed 行为仍有测试钉死。

### FIX-002 | P0 | 10 并发调查租约丢失（锁/续期争用，不是「租约设计错」）

- **位置**：[`tests/system/test_concurrency_smoke.py:68-137`](backend/tests/system/test_concurrency_smoke.py) `asyncio.gather` **无** `return_exceptions`；抛出点 [`super_agent.py:130-138`](backend/app/agents/super_agent.py) `renewal_failed` → `InvestigationLeaseLostError`。租约 [`lease.py`](backend/app/orchestration/lease.py) `DEFAULT_LEASE_TTL_S=600`，`RENEW_INTERVAL_S=60`。LLM 是 `FailingLLMClient()`，调查应短，**TTL 不够不是主叙事**。
- **触发**：CI `backend-system-test` 该测 FAILED。历史同测还打过 `DeadlockDetectedError`。更像 **锁顺序 → 变慢/续期失败 → gather 一个失败取消其余**。
- **影响**：system-test 红；同 run `backend-test` **cancelled**（CI 图，不是第二条产品 bug）。
- **故意留下的部分**：租约丢失 fail-closed（`InvestigationLeaseLostError`）好处>坏处，不要吞。
- **工业修复**：统一事务锁顺序（event / intent / outbox）；死锁 `serialization_failure` 有限次重试；续期失败只打标该 `event_id`；gather 用 `return_exceptions` 让其余事件仍能收口（失败仍要 fail 该测，但日志要分得出「1/10 丢租约」还是「9/10 被取消」）。
- **禁止**：并发改成 1；xfail；加长 sleep；取消租约 fail-closed。
- **验收**：该测 CI 连续绿；人为弄丢单事件租约仍 fail-closed。

### FIX-003 | P1（叙事/prompt，不拆门） | glm 不隔 DB；门用 rule-pool 补上后 strategy 仍写「库在线」

- **位置**：prompt [`response_prompt.py:87-97`](backend/app/agents/prompts/response_prompt.py) `Prefer lower-risk actions first.`（有意保守）。门 [`apply_containment_quality_gate`](backend/app/agents/rules/response_plan_quality_gate.py) **不再**「有任意遏制就放行」；[`_merge_entity_coverage`](backend/app/agents/rules/response_plan_quality_gate.py) 只补 EntitySet 的 host/account/外网 **dest** IP。合成 reason 本是 `"entity coverage merge"`，但 merge **优先拷贝 rule-fallback 候选**，故 Live DB isolate 的 reason 是 **`rule fallback`**——与「门没跑」不是一回事。strategy 末尾已有 `containment_quality_gate: entity_coverage_merge`。
- **本 Live**：WKS 模型自己隔；DB `reason=rule fallback`；strategy 写 *SRV-DB-STG-02 remains online*。`strict_aligned=true`，诱饵未入计划。
- **影响**：覆盖能力在；审批/报告可能读到「库继续在线」却已 isolate。
- **与 FIX-008/328**：prompt 若写成「每个 host 都 isolate」会冲库存/诱饵合同。正确口径：**EntitySet hosts** 必须有 `isolate_host`；不要扩库存。FIX-008 只去重身份工具，**不得**砍 WKS/DB isolate。
- **工业修复**：prompt 写明 `confirmed_threat` 时对 EntitySet 主机隔离，允许先低风险但不得省略已识别失陷主机；禁止 strategy 写「remains online」若该主机将被覆盖门补 isolate。回归：LLM 只隔 WKS 时 merge 后仍有 DB；prompt 夹具断言含「EntitySet host」而非「inventory host」。
- **禁止**：拆 328 merge；isolate 进 `validate_closed_gate`；对诱饵/库存盲扩。
- **验收**：再跑同一 Live，strategy 与 DB isolate 不互相否定；诱饵仍不在计划。不强制 DB isolate 的 reason 从 `rule fallback` 变成模型原文——门拷贝规则池是 328 实现，不是缺陷。

### FIX-007 | P2 | #970 循环导入（属实，但是顺序敏感；API 常走路径未炸）

- **位置**：[`workflow_graph.py:85`](backend/app/orchestration/workflow_graph.py) 顶层 `from app.services.analysis_only_pipeline import run_rag_stage`；[`orchestration/__init__.py:35`](backend/app/orchestration/__init__.py) 急切导出 `planner_node, rag_node`；pipeline 顶层 import `triage_input_builder`（触发 `app.orchestration` 包初始化）。pipeline 对 `super_agent` 的 lazy import（918–920）只拆了另一段环。
- **本机复核**：`import app.services.analysis_only_pipeline` → `ImportError: cannot import name 'run_rag_stage' from partially initialized module`。先 import `workflow_graph` / `app.orchestration` / `get_workflow_runtime` **成功**。对抗 pytest 未炸。#970 仍 OPEN，优先级本就是 P2。
- **影响**：裸 import / 部分脚本不稳定；**未证明** HTTP investigate 500。
- **工业修复**：把 `run_rag_stage` 抽到无环模块；`orchestration.__init__` 不要急切拉 graph。回归必须覆盖 **两种顺序**。
- **禁止**：只加 lazy import 仍在 `__init__` 急切导出同一符号（#970 禁止事项）。
- **验收**：`python -c "import app.services.analysis_only_pipeline"` 与反向 import 均成功。

### FIX-008 | P2 | glm 一次 12 动作含 L4 — 与 328 补 isolate 叠加

- **位置**：本 Live 计划（含 identity 三件套 + 双 isolate + 双 scan + quarantine + block）。328 merge **额外**加上模型不想要的 DB isolate。
- **影响**：脚本一键批 12 条；生产审批面大。不是状态机 bug。
- **工业修复**：同一 account 的 disable / force_logout / revoke_token **去重为一条身份链**；L4 单独批注。
- **禁止**：为少动作删 WKS/DB isolate（与 FIX-003/328 冲突）。
- **验收**：本场景身份动作 ≤2；isolate 仍覆盖 WKS+DB。

### 9.2 降级：故意设计或好处≥坏处（不要按上一版修）

### FIX-004 | 不修（ISSUE-032） | 分诊 HIGH 只认告警文本里的外网 IP

- **属实**：[`triage_agent.py:144-155`](backend/app/agents/triage_agent.py) 注释写明 ISSUE-032：data_exfil 仅当 `_external_ip_in_text(alert_text)` 才 HIGH，否则 MEDIUM。糊标题无 IP → 本 Live 分诊 medium、risk 74–75 high。
- **不实/过强**：Response user payload **已经同时给** `severity`（triage）和 `risk_severity`（high）。glm 看见了 HIGH 风险仍选择不隔 DB。把「不隔 DB」主要归到分诊没读 `normalized.src_ip` 不成立。
- **故意**：032 避免「内部外发/糊标题」被规则抬成 HIGH；330 双列对外诚实。改分诊枚举会红 032 测，并与「不要用分数卡 severity 吃掉分诊」互斥。
- **可选（非必须）**：prompt 写「遏制激进度跟 `risk_severity`，不要跟 triage medium 走」——不改 triage 合同。

### FIX-005 | 不修评分器（ISSUE-212） | `generated_by=template` → 永远 `degraded_template`

- **属实**：[`assess_report_quality`](backend/app/services/report_quality.py) 第 3 步 template 永不 complete。图内 upsert 不跑 HTTP 212 拒 incomplete（348/312）。
- **不实**：上一版修法「template + 完整章节 → complete」**直接违反 212**（测试：更丰富的模板内容不得冒充 complete）。
- **本 Live**：`report_quality=degraded_template`；ReportAgent **有** `llm_client`；摘录仍是模板 `decision_brief` 腔。日志未见 `ReportAgent LLM path failed`，根因可能是 LLM 报告路径失败被吃掉、或成功但仍落模板标记——**未钉死**。即使将来 LLM 报告成功，212 会给 `complete`；不必改评分器。
- **禁止**：为答辩把 template 改成 complete；把 212 变成 CLOSED 硬门；对抗把 `report_quality_complete` 做成 CLOSED 计分（与 348/312 冲突）。可选：查 Live 报告 LLM 为何没把 `generated_by` 打成 `llm`（观测，不是合同变更）。

### FIX-006 | 不修（ISSUE-309 / 347） | OQ 默认不挡、对抗放 unscored

- **属实**：`OUTPUT_QUALITY_BLOCKING` 默认 false（309：评估器宕机不得停 P0）。对抗分数卡把 OQ 放在 `unscored.output_quality`（347），断言 `output_quality not in checks`。本 Live triage OQ 0.565 已在 nested 产物。
- **故意**：好处（P0 不因 OQ 挂掉）> 坏处（0.565 不挡 CLOSED）。全局改 true 回潮 309。
- **可选 P2 DX**：人类摘要脚注一行 OQ warn。不要当高质量闭环 blocker。

### FIX-009 | 不修代码（产品策略） | glm 封 VPN **源** `198.51.100.44`

- **属实**：reason 写 *source*，不是 327 误标 dest。328/339 覆盖门 **故意跳过** `_BLOCK_IP_SOURCE_FIELDS`，只要求封外网 dest。
- **故意**：默认不把 VPN 出口当 exfil dest 一起封，误伤更小。glm 多封了源 IP 是模型选择，不是覆盖门漏了。
- **产品一句即可**：建议身份路径用 disable，源 IP 需显式才封。不要把「封源」写进 328 覆盖合同。

### 9.3 复核后不升级的遗漏

| 项 | 为何不进必须改 |
|----|----------------|
| Verify 只降 `adapter_acknowledged`，CLOSED 非 mock 还拒 `status_queried` | `workflow.py:542-548` 已文档化；本环 `disposition_is_mock=true` 未踩。留待 live XDR。 |
| `backend-test` cancelled | system-test 失败的 CI `needs` 后果，修 FIX-002 即恢复。 |
| 无 Docker compose 金路径 | 环境限制，保持 SUSPECTED。 |
| 高质量条「模型自己隔 DB」 | 与 328「门是安全网」对打；改为叙事对齐（FIX-003），不把「无门的模型 isolate」当闭环定义。 |

### 不改（高质量闭环也不要动）

| 项 | 为什么留下 |
|----|------------|
| 理解 0/3（334） | 糊标题本来没有实体名；echo 不算读懂。 |
| Mock `mock_simulated`（351） | 本环 DISPOSITION mock；改成假 live 验证是假闭环。 |
| `validate_closed_gate` 不查 isolate（312） | 覆盖放在 Response 门 + 对抗断言。 |
| Memory 不进图（305） | 钩子不是节点。 |
| detection 两例 fail-closed ERROR | 冷启动无 baseline / 超扫描预算必须失败，不要改成检出。 |
| `required_gate: true`（发布门数据集上） | 诚实红；冲突用拆集解决，不用关闸。 |
| ISSUE-032 分诊文本 IP | 见 FIX-004。 |
| ISSUE-212 template 非 complete | 见 FIX-005。 |
| ISSUE-309 OQ 非阻塞 | 见 FIX-006。 |
| 328 只覆盖 EntitySet、只封 dest | 见 FIX-003/009。 |

### 建议修序（复核后）

1. **FIX-002**（并发锁/gather — 真正的运行时 P0）
2. **FIX-001**（拆评测合同，不要改检测 fail-closed 语义）
3. **FIX-003**（prompt/strategy 与门对齐；保留 328）
4. **FIX-007**（抽 `run_rag_stage`，关 #970）
5. **FIX-008**（身份工具去重）
6. 再跑同一对 Live glm；CI 在合同自洽后全绿。不要为第 6 步去改 032/212/309。

### 明确不要做的

- 不要把 Mock `backend-closure-gates-mock` 绿说成 glm 遏制。
- 不要把本 Live DB isolate 说成模型主动（当前是覆盖门拷贝 `rule fallback`）。
- 不要把 `readback_verified` 字段对外说成非 mock 已验证。
- 不要把 isolate 塞进 `validate_closed_gate`。
- 不要恢复 CoT；不要把 echo 折回 `text_understanding_hits`。
- 不要让 detection cold-start/budget 切片「检出」来刷 pass_rate。
- 不要把 template 报告改成 `complete` 来答辩。
- 不要全局打开 `OUTPUT_QUALITY_BLOCKING`。
- 不要在仓库提交 LLM 密钥；对话中的 Ark key **轮换**。

---

## 10. 证据索引（无密钥）

| 项 | 位置 |
|----|------|
| AnalysisOnly 日志 | `/tmp/live_analysis_only.log` |
| full_loop 日志 | `/tmp/live_full_loop.log` |
| 产物 | `backend/tests/adversarial/artifacts/latest_*.json`（gitignore） |
| CI | https://github.com/munos1233/shadowtrace-/actions/runs/31917018845 |
| 328 门 | `backend/app/agents/rules/response_plan_quality_gate.py` `apply_containment_quality_gate` |
| 328 规则 | `backend/app/agents/rules/default_response_rules.py` DATA_EXFIL HIGH |
| detection CI artifact | run `31917018845` 工件 `detection-evaluation-run-artifact` / `detection_ci_run.json`（7 案：pass 4 / unevaluable 1 / error 2） |
| #970 复核 | `import app.services.analysis_only_pipeline` 失败；`import app.orchestration` / `get_workflow_runtime` 成功 |

---

## 11. 探查命令摘要

```
git log origin/main --oneline -8   # d524a16 ISSUE-350/328
gh run list --branch main --limit 5  # 最新 failure
cd backend && uv run --frozen ruff check app tests     # All checks passed
cd backend && uv run --frozen ruff format --check app tests  # 911 files already formatted
# mypy：本轮以 CI backend-lint success 为准，未单跑以免重复耗时
rg pytest.mark.xfail backend/tests --glob '*.py'   # 0
```
