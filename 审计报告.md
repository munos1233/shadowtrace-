# ShadowTrace 发布前深度 Bug 探查报告

| 项 | 值 |
|---|---|
| 基准分支 / commit | `main` @ `6609c79ba1bdc6eee96b96336e78e6138fa5e1c0`（`fix(issue-374): sort imports…`） |
| 探查日期 | 2026-08-17 |
| 立场 | **fail-closed**：宁可误报可疑路径，不可漏掉 bypass / 假成功 / 状态非法跳转 |
| 交付范围 | **仅问题与证据，不改产品代码** |
| 探查方式 | 静态（六维 + P0 主链路）+ 动态（本地 PG16/Redis7 + **Live glm-5.2 两条 in-process Agent 全链路** + **本机自装 Docker 后 HTTP/Celery 全链路**，含 Live glm `suspicious_domain_access` 研判质量复盘）+ GitHub Actions run `32007667156` |
| 对照旧报告 | 仓库内原 `审计报告.md` 基准为 `738c4784`（2026-08-11）。ISSUE-354–362（GH #1019–#1027）已合入；本文 **不重报已修复项**，只记录本 SHA 仍成立的缺陷 |

## 执行摘要

**结论：CI Mock pytest 金链路能 CLOSED，但 Docker HTTP 金路径与 Live glm 研判质量、当前 main CI 均未达发布门槛。不建议按现状发布。**

四条互不替代的事实：

1. **CI Mock 闭环绿 ≠ 研判能力。** `backend-closure-gates-mock` 在 run `32007667156` **success**；同 SHA 上 Live SuperAgent 全链路却在 **未封禁外泄域名** 的情况下走进 `CLOSED`。
2. **Live SuperAgent 图跑通了，质量断言没过。** `production_graph` 183.3s 到达 `closed`，Verify `overall_status=success`，终端写回 `mock_simulated`；pytest 因 `block_domain×storage-sync-cdn.example` 硬失败。Scorecard 仍打 7/7 PASS——人读 scorecard 会误判。
3. **当前 main CI 红。** `backend-lint` / `frontend-build` / `backend-test` 失败；`backend-test-gates` 因 `needs` 被 skip。contract-drift / system / regression / evaluation / docker-build / closure-gates-mock 绿。
4. **本机 Docker Live glm `suspicious_domain_access`（433s eval OK / CLOSED）不是 glm 研判成功。** 场景 GT **本来就是** `final_verdict=none` + `not_required`，risk 53 / 判定 none **对齐**。glm **9/9 timeout**。Health 50% 是 60min 窗口（契约）。`--require-closed` 是 ISSUE-301 **管道门**，接受 `degraded_template` 是故意的。CLOSE 在 `LLM_REQUIRED=false` 下合法；缺的是单独的 glm 认证门（ID-REL-007）。

| 级别 | 数量（去重后、不含已合入 354–362） |
|------|-------------------------------------|
| P0 Blocker（研判假闭环 / CI 必红 / 导入环 / Docker 金路径） | 7（BLK-001–007。**CTR-001 二次复核降为设计，不再计 P0**） |
| P1 Should-Fix（去重、不含 P0 投影） | 3（SEC-001、SEC-002、REL-007 研判认证卡） |
| P2 Nit / 设计注意 / 评测误读 | 见 §11；若干原 P1 降级 |
| 已知契约/设计（不作为未修复回归） | ISSUE-108/199/301/305/312/328/334/347/349/350/352、LLM_REQUIRED=false 等 |
| SUSPECTED | 1（DinD ICC 为运行时环境限制，非产品缺陷） |

**发布阻断优先级（二次复核后仍成立）：**

1. Live fallback 漏封 **外泄域名** 仍允许 `CLOSED`（ID-BLK-001）。修法是 **HIGH 规则并入 `block_domain`**（MEDIUM∪L3），**不要**改 ISSUE-328 coverage 契约、也 **不要**把 GT 域名塞进 `validate_closed_gate`（与 ISSUE-349/312 冲突）。
2. CI 三红：format / 前端 `simulated` / ReportAgent caplog（ID-BLK-002–004）
3. `#970` 循环导入（ID-BLK-005）——`import app.services.analysis_only_pipeline` 在本环境 **直接 ImportError**
4. `make up-demo` observability 卷路径（ID-BLK-006）；Docker HTTP required 金路径卡在 verifying（ID-BLK-007，**根因已更正**，见下）

**二次复核一句话：** 原报告把若干 **故意设计**（规则降级、scorecard 不含 coverage、`--require-closed` 只验管道、Health 60min 窗口、auto-investigate 不含 response）写成了缺陷；把 BLK-007 的根因写成了「Risk 没写判定」。详见 **§11**。

---

## 1. 元信息与探查范围

### 1.1 已合入、本文不重报（ISSUE-354–362 = GH #1019–#1027，全部 CLOSED）

| GH | ISSUE | 本 SHA 代码落点 |
|---|---|---|
| #1019 | 354 detection CI 拆分 | `.github/workflows/ci.yml` evaluation observe `--allow-gate-fail`；`data/evaluation/detection_shadow_v1*` |
| #1020 | 355 并发租约 | `backend/app/orchestration/lease.py`；`tests/system/test_concurrency_smoke.py`；system-test **绿** |
| #1021 | 356 backend-test 超时 | `ci.yml` `timeout-minutes: 45`；本 run pytest 墙钟 34:16，未再撞 35m 取消 |
| #1022 | 357 strategy vs isolate | `response_prompt.py` + `response_plan_quality_gate.py` |
| #1023 | 358 报告 15 节 all-or-nothing | `CORE_LLM_SECTION_KEYS` + merge-then-grade；**单测 caplog 仍红**（见 ID-CTR-002） |
| #1024 | 359 identity 去重 | `apply_identity_containment_dedup_gate` |
| #1025 | 360 跟随 `risk_severity` | `response_prompt.py` |
| #1026 | 361 dest-only `block_ip` | Live 本轮 **未** 封 VPN `198.51.100.44`（符合 361）；封了目的 `198.51.100.77` |
| #1027 | 362 Verify 强确认 | `CLOSED_TERMINAL_STRONG_CONFIRMATION_EVIDENCE`；Mock 路径仍接受 simulated |

### 1.2 P0 主链路（已对照代码）

```
Ingestion → EventService → investigate
  ├─ graph: SuperAgent / workflow_graph
  │    triage → planner → evidence → fp → [rag?] → graph → risk
  │    → response → approval → execute → verify → report → close
  └─ analysis_only: AnalysisOnlyPipeline（止于 REPORTING；无 response/verify）
StateMachine.validate_closed_gate + side_effect_convergence
ActionExecution → DispositionSync → VerifyAgent（两阶段）→ writeback_recovery
close/report（含 force_local_close admin 旁路）
```

### 1.3 本环境能力

| 依赖 | 状态 |
|------|------|
| Docker daemon | **补测已装并启动**（本镜像原无 docker 客户端；`apt` 安装 docker.io 29.1.3，无 systemd 故手动 `dockerd`，storage=`vfs`）。`make up-demo` 失败见 ID-BLK-006；`WORKER=1` 栈 + HTTP 全链路见附录 C |
| PostgreSQL 16 + Redis 7 | **本地可用**（`alembic upgrade head` 成功） |
| Live LLM | `LLM_MODE=openai_compatible`，`LLM_PRIMARY_MODEL=glm-5.2`，Ark `.../api/coding/v3` |
| 密钥 | 审计对话中提供了 Ark key。**禁止写入本报告或 git。该 key 已暴露，必须轮换。** |

### 1.4 动态：Agent 全链路 + Docker HTTP Live

| # | 路径 | 命令/入口 | 结果 |
|---|---|---|---|
| 1 | AnalysisOnlyPipeline | `tests/adversarial/test_agent_adversarial_audit.py` Live | **pytest PASSED**；pipeline **73.4s** → `REPORTING` |
| 2 | SuperAgent `production_graph` | `tests/adversarial/test_agent_adversarial_full_loop.py` Live | **图 CLOSED（183.3s）**；**pytest FAILED**（域名覆盖） |
| 3 | Docker HTTP Live glm | `dynamic_eval_full_loop.py --scenario suspicious_domain_access --require-closed` | **eval OK / CLOSED 433s**；研判质量 **不及格**（§2.4：9/9 timeout） |

产物（gitignored）：`backend/tests/adversarial/artifacts/latest_audit.json`、`latest_full_loop_audit.json`。日志：`/tmp/live_analysis_only.log`、`/tmp/live_full_loop.log`。

---

## 2. Live 研判质量（本轮核心证据）

### 2.1 场景 ground truth

`adversarial_credential_db_staging_exfil`：VPN 异常地理登录服务账号 → 分析工作站凭证工具 → RDP 横向到 DB staging → mysqldump → rclone HTTPS 上传到 `storage-sync-cdn.example`。

必须覆盖的处置目标：`svc-analytics-47` / `WKS-DATA-031` / `SRV-DB-STG-02` / dest IP `198.51.100.77` / **域名 `storage-sync-cdn.example`（block_domain）**。VPN 源 `198.51.100.44` **不得**默认 `block_ip`（ISSUE-361）。

### 2.2 Loop 1 — AnalysisOnly（研判半程）

| 项 | 观察 |
|---|---|
| event_type / risk / verdict / triage | `data_exfiltration` / **74** / `confirmed_threat` / medium |
| 终态 | `REPORTING`（无 response，符合 analysis_only 契约） |
| report_quality | `degraded_template`（不挡 CLOSED；本路径也不走 CLOSED） |
| text_understanding | **0/3 entities，0/2 IOC**（ISSUE-334 不透明告警设计；`source_projection` 有名字） |
| OutputQuality triage | **0.565 warn**（specificity 0、grounding 0.30） |
| 分诊摘要 | Live 文本提到 svc-analytics-47 / WKS / SRV-DB / storage-sync-cdn.example（理解在自然语言里，不进 scorecard 实体槽） |

**研判评语：** 类型/风险/判定可用；报告模板降级；实体槽仍空。AnalysisOnly 不能证明封禁覆盖——必须看 Loop 2。

### 2.3 Loop 2 — SuperAgent 全链路（含审批/执行/Verify/写回/CLOSED）

| 项 | 观察 |
|---|---|
| 图耗时 / investigate | 183.3s / `completed` |
| 状态序 | `new→triaging→collecting_evidence→analyzing→scoring→planning_response→waiting_approval→executing_response→verifying→reporting→closed` |
| node_trace | triage…rag…graph…risk…**response…approval…execute…verify…report…close**（P0 节点齐） |
| risk / verdict | **79** / `confirmed_threat` |
| `response_plan_generated_by` | **`template`**；strategy **`DEFAULT_RESPONSE_RULES fallback`**（LLM 空候选，非 exception 日志） |
| 7 个动作（全部 `rule fallback`） | `create_ticket`、`notify_security_team`、`block_ip(198.51.100.77)`、`disable_account(svc-analytics-47)`、`isolate_host(WKS-DATA-031)`、`isolate_host(SRV-DB-STG-02)`、deferred 终端 disposition |
| **缺失** | **`block_domain(storage-sync-cdn.example)`** |
| ISSUE-361 | VPN `198.51.100.44` **未**入计划（正确） |
| Verify | `overall_status=success`；实体 effect `verified`；终端激活后写回 |
| 写回认证 | `simulated=true`，`certification_label=mock_simulated`（诚实，未冒充 live） |
| pytest | `assert prod["disposition_targets_aligned"]` **失败** |
| scorecard | `score.passed=7/7`，`verdict_for_human` 仍写 **PASS** 并附 coverage GAP |

Storyline：`StorylineService LLM path failed … LLM request timed out` → 规则回退。

**研判评语（fail-closed）：** 账号/双主机/目的 IP 封禁来自规则+ISSUE-328 merge，看起来像完整遏制；**外泄通道域名未封却 CLOSED**。这是「状态机真闭环 + 遏制假完整」。Mock 金脚本不会暴露这条，因为 Mock LLM 会吐出含 `block_domain` 的计划。

### 2.4 Docker HTTP Live glm — `suspicious_domain_access` 研判质量（本轮补测）

命令（`--require-closed`，`seed=7`，`max-wait-s=600`）：

`python3 scripts/dynamic_eval_full_loop.py --seed-via-compose --scenario suspicious_domain_access --seed 7 --require-closed --base-url http://127.0.0.1:18000 --token bootstrap-token --mock-xdr-url http://172.18.0.1:18100 --max-wait-s 600`

eval：**OK**，`elapsed_s=433.4`，`status=closed`。事件 `evt-20240615-a21c0948`。容器 `LLM_TIMEOUT_SECONDS=120`（triage / RAG 仍走 ISSUE-251 的 15s 短超时；ReportAgent 硬编码 30s）。

#### 质量拆分（不要把 eval OK 当成 glm 成功）

| 层 | 判定 | 依据 |
|---|---|---|
| HTTP/Celery 管道 | **PASS** | `new→…→closed`；`closed_at=2026-08-17T09:18:10Z`；`--require-closed` 脚本退出 0 |
| 场景语义 / GT | **PASS** | 见下表。`final_verdict=none` **是该场景设计值**，不是漏判 |
| glm-5.2 研判 | **FAIL / 无法评估** | 本事件 9/9 `llm_timeout`、0 token；所有 Agent 靠规则/模板 `completed` |
| 报告可读质量 | **FAIL** | `generated_by=template`，`report_quality=degraded_template`，warnings=`report_llm_fallback:llm_timeout` |
| 评测门禁 `--require-closed` | **管道 PASS / 研判不验** | ISSUE-301 故意；不是 glm 门 |
| Health LLM 成功率 | **窗口 50% ≠ 本事件 0%** | 60min 契约；评测误读 |

**总评：** 这条 Live 只能证明「规则引擎在可疑域名 MEDIUM 场景上能 CLOSED」。**不能**证明 glm-5.2 会分诊、打分、写处置或写报告。433s 几乎全是超时空等，不是模型思考时间。

#### 场景 GT vs 观察（判定 none 不是缺陷）

`backend/app/data_generators/scenarios/suspicious_domain_access.py:159-209`：`expected_verdict=none`，`expected_severity=medium`，`risk_score=55`（允许带 40–69），`disposition_policy=not_required`，`exfil_observed=False`。系统期望 `backend/tests/system/scenario_expectations.py:107-125`：`FinalVerdict.NONE`，rule-fallback 风险 40–60，允许动作 `block_domain` / `create_ticket` / `notify_security_team`，**不含** `isolate_host`。

| 项 | GT / 规则 | 本事件观察 |
|---|---|---|
| event_type / severity | `suspicious_domain` / medium | 同左（`degraded_flags` 含 `event_type_from_heuristic=suspicious_domain`） |
| risk | 源基线 55；rule-fallback 40–60 | **53**；`scoring_mode=rule_only`，`llm_admissibility=invalid`，confidence≈0.767 |
| final_verdict | **none** | **none** |
| disposition | not_required；无终端写回 | `writeback_required=false`，`writeback_readiness=not_required` |
| 处置 | MEDIUM 规则：`block_domain` + ticket（`default_response_rules.py:174-178`） | `block_domain(brand-new-cdn-example.net)` + `create_ticket`，reason 均为 **`rule fallback`**；Verify `check_domain_block_status` effect **verified**；**未** isolate 办公主机（正确） |
| 报告 | 期望有报告 | `degraded_template`；摘要写「Text entity extraction empty; using structured source entities」；证据 9 条、缺口 0 |

#### glm 调用：本事件 0% 成功

`llm_call_log`（事件 `evt-20240615-a21c0948`，全部 `error_class=timeout`，token 全 0）：

| agent | prompt_key | latency_ms | 墙钟（UTC） |
|---|---|---|---|
| triage_agent | triage_extract | 15015 | 09:11:17 |
| planner_agent | plan_generate | 120019 | 09:13:17 |
| RAGAgent ×3 | query_rewrite | 15016–15019 | 09:13:35（并行） |
| risk_agent | risk_score | 120004 | 09:15:36 |
| response_agent | response_plan | 120036 | 09:17:37 |
| report_agent | report_generate | 30000 | 09:18:10 |
| storyline_service | storyline_generate | 120038 | **09:19:38（CLOSED 之后）** |

超时等待合计：15+120+15+120+120+30 ≈ **420s**，与 eval **433.4s** 吻合。`closed_at=09:18:10` 紧接 report 超时；**storyline 在 CLOSED 后再空等 120s**，eval 已 OK，调用仍计入后续 Health 窗口。

Response artifact：`generated_by=template`，`strategy_summary=DEFAULT_RESPONSE_RULES fallback`。Risk artifact：`scoring_mode=rule_only`。Agent task `risk_score` / `response_plan` 状态均为 `completed`（超时被当成可继续）。

#### Health 50% 不是本事件成功率（ID-CTR-007，P2 评测误读）

`/api/v1/health` → `llm.audit` 按 `LLM_AUDIT_WINDOW_MINUTES=60` 对 **全表** `llm_call_log` 做滚动聚合（`diagnostics.py:116-160`），**不分 event_id**。这是 health 契约，不是实现漏过滤。eval 结束瞬间：前一事件成功调用 8 次 + 本事件已落盘超时 8 次（storyline 尚未写入）= **8/16 = 0.5**。本事件自身是 **0/9**。不要把 Health 当 glm 场景断言。

#### `--require-closed` 对本场景是管道门（ID-CTR-008，ISSUE-301 故意）

- 场景 profile：`semantic=analysis_only_domain`，`pressure=full_loop_compat`。全局 `--require-closed` 只检查 `status=closed` + GET `/report` 且不是 `incomplete_placeholder`。
- `writeback_required` 为 false 时跳过 CONFIRMED（ISSUE-302）。`degraded_template` **通过**（ISSUE-212 合法降级报告）。
- 「eval OK」= 管道 CLOSED + 有报告壳。**不证明** glm 成功。该场景 GT 本来也不要求 threat。不要改 strict 去拒绝 `degraded_template`（与 main 契约冲突）。

#### 次要降级信号（不改变「规则对齐 GT」）

`degraded_flags`：`event_type_from_heuristic=suspicious_domain`；`state_transition_projection_degraded=side_effect_convergence:returned_degraded|proj=audit:28`；`redis_context_unavailable=true`（本 DinD hairpin overlay 环境，不升产品 P0）。投影已 degraded 仍允许 CLOSE。

---

## 3. 维度 1 — 重大代码硬性 Blocker

### ID-BLK-001 | P0 | Live HIGH 规则回退漏 `block_domain`，CLOSED 门禁不拦

- **位置**
  - `backend/app/agents/rules/default_response_rules.py:103-127` — `DATA_EXFILTRATION` **MEDIUM** 有 `block_domain`；**HIGH 没有**；CRITICAL 才再有
  - `backend/app/agents/response_agent.py:757-759` — `severity = max(risk_severity, triage_severity)`；本轮 risk=high → HIGH 池
  - `backend/app/agents/response_agent.py:1008-1044` — LLM 空候选 → `DEFAULT_RESPONSE_RULES fallback`
  - `backend/app/agents/rules/response_plan_quality_gate.py:137-197` — `entity_containment_coverage_needs` **只覆盖 account / host / 外部 dest IP，明确不含 domain**
  - `backend/app/models/workflow.py:935-1088` — `validate_closed_gate` 查报告 + side-effect 收敛 + 终端 CONFIRMED，**不查 EntitySet/GT 域名覆盖**
- **现象**：glm-5.2 未产出 Response 计划时，HIGH 规则封 IP/账号/主机，**跳过外泄域名**；Verify success；事件 `CLOSED`。对抗 pytest 是唯一硬失败。
- **根因（复核后更准）**：不是「HIGH 嫌 block_domain 档次低」。`response_agent.py:809-814` 在 containment 需要时把 DATA_EXFIL **MEDIUM 池整表换成 HIGH 池**，目的是补 L3（isolate/disable）。MEDIUM 有 `block_domain`，HIGH **没有**，CRITICAL 又有——HIGH 不是 MEDIUM 的超集。ISSUE-328 merge **故意**只补 host/account/dest IP（`test_has_actionable_includes_domains_but_coverage_needs_ignore_them` 断言 coverage 忽略 domain）。`validate_closed_gate` **故意**不看 GT 覆盖（ISSUE-312/347/349）。
- **证据**：`latest_full_loop_audit.json` `disposition_coverage_gaps=["block_domain×storage-sync-cdn.example"]`；`response_plan_generated_by=template`；pytest `test_agent_adversarial_full_loop.py:614-617`。`test_data_exfiltration_high_rules_include_required_tools` 用 `in names` 超集断言，**加上 `block_domain` 不会破该测**。
- **修复建议（不与 main 契约打架）**：HIGH `DATA_EXFILTRATION` 并入 `block_domain`（或 lift 时做 MEDIUM∪HIGH，而不是替换）。GT 覆盖继续由对抗 `production_checks.disposition_targets_aligned` 硬门负责。
- **不要修**：把 domain 写进 `entity_containment_coverage_needs`（破 ISSUE-328 单测）；把 coverage 写进 `validate_closed_gate` / 让 scorecard 因 GAP 变 FAIL（破 ISSUE-349 与 `test_adversarial_helpers` 里大量 `PASS`+`coverage GAP` 断言）。

### ID-BLK-002 | P0 | CI `backend-lint`：ruff format

- **位置**：`backend/tests/test_services/test_issue277_baseline_resume.py:186-188`
- **现象**：`ruff format --check` 1 file would be reformatted（`assert any(` 生成器折行）。本地与 CI 一致。
- **根因假设**：ISSUE-277 测试合入未跑 format。
- **证据**：run `32007667156` job `backend-lint` 失败日志；本机 `uv run ruff format --check app tests`。
- **修复建议**：对该文件 `ruff format`（本报告不改产品/测试代码）。

### ID-BLK-003 | P0 | CI `frontend-build`：`EventWriteback.simulated` 缺失

- **位置**
  - 类型：`frontend/src/types/event.ts:839-846`（`simulated: boolean` **必填**）
  - 夹具：`frontend/tests/utils/eventTodos.test.ts:148-159` **无** `simulated`
- **现象**：`tsc` TS2741，`frontend-build` 红；`frontend-e2e` skip。
- **根因假设**：Writeback 类型收紧后夹具未跟。
- **证据**：CI Typecheck `error TS2741: Property 'simulated' is missing … required in type 'EventWriteback'`。
- **修复建议**：夹具补 `simulated: true/false`。OpenAPI 该字段有 default、不在 required——契约松、TS 严（见 ID-CTR-003）。

### ID-BLK-004 | P0 | CI `backend-test`：ISSUE-358 caplog 断言

- **位置**：`backend/tests/test_agents/test_report_agent.py:1000-1028`；实现 `backend/app/agents/report_agent.py:271-286`
- **现象**：`test_llm_whitespace_core_is_template_partial_sections`：行为断言（`generated_by=template`、`degraded_template`、warnings）在 CI traceback 中已过，**仅** `assert any("missing_core" in rec.message)` 失败。汇总 **1 failed, 5381 passed**（34:16）。`backend-test-gates` skipped。
- **根因假设**：caplog logger 过滤器与 `ReportAgent.execute` 包装 logger 不一致（CI 与本地偶发差）。行为本身像已实现 358。
- **证据**：run `32007667156` job `95320464537`。
- **修复建议**：断言 `report.warnings` / `generated_by`，不要绑死 log 子串；或对 `caplog` 用 root logger。

### ID-BLK-005 | P0 | `#970` 循环导入：`analysis_only_pipeline` 直接 import 失败

- **位置**
  - `backend/app/orchestration/__init__.py:35` — `from app.orchestration.workflow_graph import planner_node, rag_node`
  - `backend/app/orchestration/workflow_graph.py:85` — `from app.services.analysis_only_pipeline import run_rag_stage`
  - `backend/app/services/analysis_only_pipeline.py:56` — `from app.orchestration.triage_input_builder import …`（会先执行 package `__init__`）
- **现象**：`uv run python -c "import app.services.analysis_only_pipeline"` →  
  `ImportError: cannot import name 'run_rag_stage' from partially initialized module 'app.services.analysis_only_pipeline'`。  
  对抗 AnalysisOnly **仍能跑**（pytest 先加载了别的模块，环被「碰巧」解开）——**导入序依赖，不是已修复**。
- **根因假设**：`run_rag_stage` 抽到 pipeline 后，`orchestration.__init__` 仍急切导入 `workflow_graph`。
- **证据**：本机复现；GitHub **#970 OPEN**。
- **修复建议**：`__init__.py` 不要急切导出 graph 节点；或 `run_rag_stage` 迁到无环模块（如 `rag_stage.py`）；加「冷启动 import pipeline」单测。

### ID-BLK-006 | P0 | `make up-demo`：observability bind-mount 解析到错误目录

- **位置**：`Makefile:172-174` 使用 `-f infra/docker-compose.yml -f infra/observability/docker-compose.observability.yml`；后者卷为 `./prometheus.yml`、`./otel-collector-config.yaml`（`infra/observability/docker-compose.observability.yml:13-15, 23-24`）
- **现象**：Compose 2.40 把相对路径解析为 **第一个 compose 文件所在目录** `infra/`，于是去挂 `/workspace/infra/prometheus.yml`。该路径不存在时 Docker 建成**空目录**，再挂到容器内文件路径失败：`not a directory: Are you trying to mount a directory onto a file`。`otel-collector` Restarting。
- **根因假设**：多 `-f` 未设 `--project-directory`，第二文件的 `./` 不以自身目录为根。
- **证据**：本机 `make up-demo` 2026-08-17 失败日志；`infra/prometheus.yml` 被创建成 root 拥有的空目录（已清理）。
- **修复建议**：observability 卷改成相对仓库根的绝对 compose 路径，或 Makefile 加 `--project-directory` / 把 observability 文件里的 `./` 改成 `./observability/...`（相对于 `infra/`）。加一条 CI/`make up-demo` smoke。

### ID-BLK-007 | P0 | Docker HTTP 金路径（required）卡在 verifying（根因已更正）

- **位置**
  - `backend/app/core/llm/scenario_context.py:16-18` — 无 `scenario` 则 Mock 走 `default.json`（ISSUE-199 **故意**，生产不硬编码 demo 名）
  - `backend/app/core/llm/golden/response_plan/default.json` — ticket-only「人工复核、不自动遏制」
  - `backend/app/core/llm/golden/response_plan/insider_data_exfiltration.json` — 才有 isolate/reset（本事件 **没用到**）
  - `backend/app/agents/risk_agent.py:189-215` — `risk_score < 70` → `FinalVerdict.NONE`（**故意**）
  - `backend/app/services/event_disposition_service.py:247-251` — `disposition is None` 也标 `effect_not_ready`（**命名不实**）
  - `backend/app/agents/verify_agent.py:1020-1035` — 非 already_submitted 的 skip → `MANUAL_RESOLUTION`
- **现象（仍成立）**：`dynamic_eval_full_loop.py --require-closed --scenario insider_data_exfiltration`（Mock LLM）事件 `evt-20240615-420918b5` 停在 `verifying/manual_resolution`，eval 240s FAIL。
- **DB 实锤（二次复核）**：`event_type=insider_threat`（期望 `data_exfiltration`）、`risk_score=57`、`final_verdict=none`、`disposition_policy=required`。LLM **8/8 success**（2–4ms），`scoring_mode=llm_and_rule`，`llm_admissibility=valid`。动作：`create_ticket` success（reason=**Conservative default — track for human review without automated containment**）+ 延迟 `update_source_event_disposition` 仍 `approved` 未激活。`source_object.normalized.scenario` **为空**。
- **原报告不实处**：不是「Risk 没写判定 / Mock LLM 失败」。Risk 写了 57；判定 none 是 **<70 分契约**。也不是「脚本只批了 1 个动作」导致写回 404 那么简单——延迟处置已 approved，激活阶段被 skip。
- **根因链**：HTTP 种子 **未把 `normalized.scenario=insider_data_exfiltration` 打进 ingested source** → Mock 全员 `default.json` → 保守工单 + 中低分 → none。场景仍是 `disposition_policy=required`。Phase 2 解析不出终端 disposition，却返回 `skipped_reason=effect_not_ready`（名不副实）→ Verify `MANUAL_RESOLUTION` **永不离开 verifying**。
- **设计 vs 缺陷**：required 且无 CONFIRMED 写回 **不得 CLOSE**——这点是 ISSUE-302 故意 fail-closed，**本事件没 CLOSED 是对的**。P0 是：(1) demo HTTP 金路径没打 scenario 标，Mock 剧本与 `SCENARIO_EXPECTATIONS` 脱节；(2) 无解 disposition 时卡死 verifying 而不是进可退出的 FAILED/REPORTING；(3) `effect_not_ready` 误名。
- **修复建议**：ingest/seed 把 `normalized.scenario` 写入 source（或 eval 显式 override），让 Mock 命中 `insider_data_exfiltration.json`；`disposition is None` 不要叫 `effect_not_ready`；required+none 应 fail-closed 到 FAILED/人工队列，而不是无限 verifying。**不要**为了让 eval 绿而允许 required 无写回 CLOSE。

### 本维已核实「不是静默吞状态机」

- `validate_closed_gate` 从 DB 投影重建，非法 CLOSED 抛 `InvalidStateTransitionError`（`workflow.py:935+`，`state_machine_service.py`）。
- `disposition_policy=required` 无终端 CONFIRMED **不能**合法 CLOSE（除 admin `force_local_close`，见 ID-SEC-001）。
- Verify `already_submitted` / `concurrent_head_conflict` 仍走回执评估，不直接 SUCCESS（`verify_agent.py:1008-1039`）。
- Mock 成功带 `simulated=true`；非 mock 拒绝 simulated 回执（`workflow.py:1052-1067`）。本轮认证标签 `mock_simulated`，**未冒充 live**。
- 出站 Disposition 有 `OutboundDispositionGuard`（分析正文/prompt/evidence raw 白名单）。静态成立；本轮未抓包 Adapter。

---

## 4. 维度 2 — Bypass / Dead Code / No-op Path

### ID-BYP-001 | ISSUE-328 覆盖门 **故意** 不含 domain（不是漏写）

- **契约**：`entity_containment_coverage_needs` 文档与单测写明只扫 hosts/accounts/external dest IPs（`response_plan_quality_gate.py:137-141`；`test_has_actionable_includes_domains_but_coverage_needs_ignore_them`：`has_actionable` 为 True，`coverage_needs` 为空）。
- **与 BLK-001 的关系**：328 **不会**在 LLM 空时补 `block_domain`。外泄漏封的可修点是 **HIGH 规则超集**（BLK-001），不是把 328 改成 ISSUE-198。
- **判定**：设计。好处=覆盖门可测、不把 IOC 域名当成 L3 主机义务。坏处=与对抗 GT 的 domain 硬门分层，人容易以为「328 修完就含域名」。**不升 P0、不建议改 328 契约。**

### ID-BYP-002 | ResponseAgent 空候选 → 规则（故意降级）

- **实际**：`llm_candidates` 空列表不走 `except`，无 `ResponseAgent LLM path failed` 日志，直接 `DEFAULT_RESPONSE_RULES fallback`（`response_agent.py:1008-1044`）。超时/异常才打 warning。这是 **LLM 降级设计**，`LLM_REQUIRED` 默认 false。
- **有害条件**：仅当规则表不覆盖 GT（BLK-001）。§2.4 域名场景规则对齐 GT，此路径 **有益**（超时仍能封域名+工单）。
- **判定**：设计。好处=LLM 宕机 SOC 不停。坏处=看起来像 glm 成功。不单独作为「应禁止 CLOSE」的 P1。

### ID-BYP-003 | Auto-investigate 不含 response/report（ISSUE-108/352 设计）

- **实际**：intent 写 `include_response_execution=False`、`generate_report=False`（`investigation_intent_service.py:545-559`）。`schedule_dispatch` 非 Celery 直接 return（595-596）。
- **文档**：Makefile `bootstrap-demo` 明确 `BOOTSTRAP_INCLUDE_RESPONSE=false`，事件不到 Approve→CLOSED；金路径是 `bootstrap-demo-full-loop` / `demo-full-loop`。
- **判定**：故意。好处=摄取自动调查不会自动 L3 遏制。坏处=与 HTTP 全闭环不是一条路。从 P1 bypass **降为设计**。Celery 才调度与 REL-003 同源。

### ID-BYP-004 | ConvergenceGuard 不可用时 replan 继续（ISSUE-062 设计）

- **实际**：捕获后打 `convergence_guard_degraded` 再 `execute_replan`。注释：safeguard, not a hard gate。
- **判定**：故意。好处=守卫挂了不把调查卡死。坏处=预算守卫失效时 fail-open。有 `ReplanCountExceededError` 仍可 escalate。从 P1 **降为 P2 设计注意**。

### ID-BYP-005 | `OutputGuard is None` 直接放行

- **实际**：`base.py:269-277`。生产 DI 有 guard；单测/ad-hoc 构造可绕过。
- **判定**：测试便利，不是运行时旁路。从 P1 **降为 P2**。

### ID-BYP-006 | MemoryAgent 不是 graph 节点（ISSUE-305，设计如此）

- **预期（营销「12 Agent」）**：Memory 在调查图内。
- **实际**：`P0_GRAPH_NODE_TO_AGENT` 无 `memory_agent`；`PLAN_STEP_ASSIGNABLE_AGENTS` 排除 memory/tool，不可执行步抛 `plan_step_not_executable`（fail-closed）。Memory 仅 post-analysis / post-close hook。
- **判定**：**不是静默 no-op**；与方案 ISSUE-080 一致。列为光学/答辩差异，不升 P0。

### ID-BYP-007 | FalsePositiveMatcher **已接入**（否定旧报告死代码说辞）

- TriageAgent post-hook + `deps.py` DI。`test_fp_matcher_e2e` 在 backend-test 进度条中跑过。不作为 bypass。

### ID-BYP-008 | HTTP investigate 默认 defer response（ISSUE-352 安全默认）

- `events.py:1314`：body 缺省 `include_response_execution=False`。required CLOSE 被 `route_after_report` 挡住。
- **判定**：故意 fail-closed（避免 GET 调查就自动 L3）。金路径脚本显式传 `true`。从 P1 **降为 P2 产品陷阱**（调用方漏参数会停在 REPORTING）。

---

## 5. 维度 3 — 契约漂移与测试假象

### ID-CTR-001 | 设计（ISSUE-349）| Scorecard 7/7 PASS 与 pytest 失败 **不是契约漂移**

- **契约**：`audit_report.py:140-142` — coverage/understanding 在 **unscored** 桶，只注解 PASS 字符串，**不改 scored 维度、不进 CLOSED**。README：`production_checks` **不覆盖** `verdict_for_human`。`test_adversarial_helpers.py` **断言** `startswith("PASS")` 且含 `coverage GAP`。
- **现象仍在**：人读 JSON 会以为 Live 通过；pytest `disposition_targets_aligned` 才是 GT 硬门（这次硬失败，门是有效的）。
- **判定**：故意分层。好处=scorecard 验管道/CLOSED，GT 覆盖另开硬断言。坏处=答辩误读。从 **P0 降为答辩风险**（已有 ID-DEMO-003）。**不要**改 scorecard 让 GAP→FAIL，会与 main 上 ISSUE-349 测试冲突。

### ID-CTR-002 | P1 | ISSUE-358 行为测绿、log 测红（测试假红）

- 见 ID-BLK-004。核心降级语义在 CI traceback 中已成立；门禁被 caplog 绑死。

### ID-CTR-003 | P1 | OpenAPI `WritebackResponse.simulated` 非 required vs 前端必填

- OpenAPI：default false、不在 required。
- TS：`simulated: boolean` 必填 → 夹具漏字段 CI 红（ID-BLK-003）。
- `event_context_snapshot` OpenAPI 为 opaque object；前端把 receipt 当结构化类型。

### ID-CTR-004 | P2 | contract-drift 本 SHA **绿**

- 本地 `check_contract_drift.py` 与 CI job 均 success。旧报告 ID-BLK-001（ISSUE-302 字段未导出）**已过时**，不重开。

### ID-CTR-005 | 零 xfail；默认 suite 用 marker 剔除重活

- `pyproject.toml` `-m 'not … adversarial_audit …'`。P0 Mock 全闭环在 **独立 job** `backend-closure-gates-mock`（本 run 绿）。
- skip 几乎全是 `postgres/redis not reachable`，不是禁用 P0。
- **风险**：默认 `backend-test` 绿不能代替 Live 对抗；而 Live 对抗不在 PR CI（ISSUE-350 已声明 Mock plumbing ≠ glm）。

### ID-CTR-006 | AnalysisOnly vs SuperAgent 双路径

- `ORCHESTRATION_MODE=analysis_only` 拒绝 `include_response_execution`。
- 本轮两条都跑了：AO 止于 REPORTING；graph 走满 execute/verify/close。行为不一致是 **设计**，但 HTTP 调用方混用会 4xx/停在 REPORTING——需在 API 错误里写清，否则像故障。

### ID-CTR-007 | P2 | Health LLM `success_rate` 是 60 分钟窗口（契约如此，不是实现 bug）

- **契约**：`LLMProviderHealth.json` / `diagnostics.py:116-160` 写明 rolling `window_minutes`，**不分 event_id**。`LLM_AUDIT_WINDOW_MINUTES` 默认 60。
- **现象**：当时 50% = 窗口内他事件成功行 + 本事件超时行；本事件 0/9。schema 已有 `window_minutes`，不是藏字段。
- **判定**：运维滚动窗口 **好处大于** 按事件切片（health 不是评测 API）。原 P1 是 **审计误读**。评测不要把 `/health` 当场景断言。增强 per-event 是可选能力，不是必修缺陷。

### ID-CTR-008 | P2 | `--require-closed` 弱是 ISSUE-301 管道门，不是研判门

- **契约**：`strict_closed_acceptance.py` 文档：CLOSED + 报告 + **若** `writeback_required` 则 CONFIRMED。明确只拒 `incomplete_placeholder`，**接受 `degraded_template`**（ISSUE-212 合法降级报告）。`not_required` 跳过写回是 ISSUE-302 quick-close。
- **场景 profile**：`suspicious_domain_access` → `analysis_only_domain` + `full_loop_compat`。全局 `--require-closed` 套在该场景上，本来就不验 glm。
- **判定**：命名像金路径质量门，实际是管道门。**好处**=LLM 降级仍能验收 CLOSED 管道。原 P1 修复建议「拒绝 degraded_template / 0% LLM 就 fail」会与降级架构和 `LLM_REQUIRED=false` **冲突**。保留为评测误用说明（§2.4），不作为产品缺陷单开 P1。

---

## 6. 维度 4 — 安全、权限与数据泄露

### ID-SEC-001 | P1（脚枪） | `force_local_close` 跳过 CLOSED 门禁（admin 逃生，故意）

- **威胁**：无 CONFIRMED 写回的本地 CLOSED，`external_unsynced=True`。
- **位置**：`events.py` admin bypass；`docs/deployment.md` 写明 bootstrap-token **含 admin**，仅运维逃生。
- **判定**：故意逃生舱。好处=现场卡死可收口。坏处=演示用 bootstrap-token 能打出「假正规 CLOSE」。保留 P1 是因为 **默认 token 过宽**，不是建议删掉 force_close。

### ID-SEC-002 | P1（配置） | `DEV_AUTH_TOKENS` 在非 production 生效

- **位置**：`config.py:643-657` 仅 `APP_ENV=production` 拒绝；`auth.py:151-158` production 直接忽略 dev token。
- **利用条件**：`APP_ENV` 不是 production（Compose 默认 development）。
- **严重级别**：生产 fail-closed 已做；**演示/staging 误配 APP_ENV** 会留下 admin bearer。P1 运维，不是代码绕过 production 闸。

### ID-SEC-003 | P2 | `Principal.has_any_role`：admin 满足一切角色闸

- `auth.py` admin 可走 approve/reject/analyst。bootstrap-token 全能。答辩用 e2e-token 更安全。

### ID-SEC-004 | P2 | 租约冲突吞成 HTTP 202

- **位置**：`events.py:1360-1367` — `InvestigationInProgressError` 不转 409，intent 已落库。
- **威胁**：客户端以为调查已调度，实际 dispatch 延期。语义偏产品而非越权。
- 355 并发租约本身已修；这是 API 语义。

### ID-SEC-005 | 出站分析内容（静态通过）

- DispositionCommand 白名单 + Mock/HTTP adapter 拒分析键。本轮未发现 raw report 出站代码路径。**未做流量抓包** → 动态标已测静态。

### ID-SEC-006 | 本报告 / git 不含 LLM key

- 对话中的 Ark key **必须轮换**。不要写进 `.env` 提交。

---

## 7. 维度 5 — 可靠性与竞态

### ID-REL-001 | 设计 | LLM 超时/空响应不挡 CLOSED（`LLM_REQUIRED=false`）

- 容器与默认配置 `LLM_REQUIRED=false`。Health 仅在 `llm_required` 时因 LLM 把整体标 degraded（`health.py:236-254`）。规则/模板降级是主路径。
- **与 BLK-001**：降级本身不是洞；HIGH 规则漏 domain 才是洞。
- **与 REL-007 / §2.4**：域名场景降级 **对齐 GT**，拦 CLOSE 会把故意的降级拆掉。

### ID-REL-002 | P0 | Verify 卡死 verifying（=BLK-007，根因见上）

- 见 **ID-BLK-007**。不是 job 未完成；Mock LLM **成功**走了 `default.json`，risk 57 → none，required 无法激活终端写回，skip 却叫 `effect_not_ready`。
- **没 CLOSED 对 required 而言是对的**；P0 是卡死 verifying + demo 种子没打 scenario。

### ID-REL-003 | P2 | 默认 `TASK_MODE=background` 重启丢任务

- `config.py:678-679` production 禁止 background。Compose/Makefile 默认 background。
- README 已写「易失」。Demo 若不用 `make up-demo`（Celery）会丢调查。

### ID-REL-004 | Outbox / 租约 / 幂等（本 SHA 静态通过）

- 单 active EVENT_STATUS_UPDATE head + FOR UPDATE supersede。
- Outbox 过期 LEASED → PAUSED（非盲重试）。
- ActionExecution 回收后 ISSUE-220 UNKNOWN，避免二次 Provider 调用。
- Redis `degraded_flags` 仅 `redis_context_unavailable` 自愈，其余手清。

### ID-REL-005 | P2 | Checkpointer / replan 有恢复，守卫 fail-open

- Redis checkpointer + `graph_resume.py`。与 ID-BYP-004 叠加。

### ID-REL-006 | CI backend-test 墙钟

- 本 run 34:16 / 45m 限额，余量约 10m。356 的超时墙已缓解；再增集成测试仍可能逼近。

### ID-REL-007 | P1（研判认证，不是状态机） | Docker Live 9/9 timeout 仍 CLOSED

- **现象仍成立**：`evt-20240615-a21c0948` 9/9 `llm_timeout`、0 token；规则对齐 GT 后 CLOSED；eval 433s OK。
- **与 §2.4 不再冲突**：管道/GT **PASS**；glm **无法评估**。`LLM_REQUIRED=false` 时 CLOSE 是 **设计允许**。
- **失败模式（质量卡）**：把这条当「glm 研判成功」会假认证。墙钟≈串行 timeout；storyline 在 `closed_at` 之后还跑。
- **不要修成**：本事件 0% LLM 就禁止 CLOSE / 让 `--require-closed` 拒 `degraded_template`——与降级架构、ISSUE-301、该场景 GT=`none` 冲突。
- **可修**：Live 认证另开门（per-event `llm_call_log` 成功率）；超时后短路后续 LLM；storyline 不要在 CLOSED 后阻塞 120s。

---

## 8. 维度 6 — 可维护性与答辩风险

### ID-DEMO-001 | P0 | 官方 `make up-demo && make demo-full-loop` 本机未通

- **答辩风险**：按 README 第一条命令当场失败。
- **复现步骤**
  1. `make up-demo` → ID-BLK-006（prometheus.yml 挂载）。
  2. 改用 `make up WORKER=1 SCHEDULER=1` 后，默认 `MOCK_XDR_URL=http://mock-xdr:8100` 在本 DinD 下 ICC 超时；hairpin 后 Mock required 场景 **停在 verifying**（ID-BLK-007：种子无 scenario → `default.json`）。没 CLOSED 对 required 无写回是对的。
- README 金路径是 `make up-demo && make demo-full-loop`，不是 `make up && make bootstrap`。

### ID-DEMO-002 | P0 | 当前 main CI 红，答辩打开 Actions 即翻车

- **复现**：https://github.com/munos1233/shadowtrace-/actions/runs/32007667156  
  红：`backend-lint`、`frontend-build`、`backend-test`。  
  绿：closure-gates-mock、system、regression、evaluation、contract-drift、docker-build。

### ID-DEMO-003 | P0 | Live 全链路 pytest 红 vs 事件 CLOSED

- **复现**：Live glm + `test_agent_adversarial_full_loop.py`。评委若只看 EventStatus 会以为成功；看 pytest / gaps 则失败。
- Scorecard 7/7 PASS 是 ISSUE-349 故意（coverage 不进 scored）；评委若只看 JSON 仍会误判，故保留答辩风险。GT 硬门是 pytest，这次它 **红了**，不是没门。

### ID-DEMO-004 | P1 | `#970` 现场 `import` 演示可能直接炸

- `uv run python -c "import app.services.analysis_only_pipeline"`。

### ID-DEMO-005 | P2 | 报告 `degraded_template` + Storyline 超时

- 15 节模板降级、攻击故事线规则回退。Live「LLM 写报告」叙事与 artifact 不符。
- Docker Live 域名闭环把这一点打满：报告明确 `report_llm_fallback:llm_timeout`，storyline 在 CLOSED 后才超时。答辩若展示 433s CLOSED 当 glm 成功会当场被 DB 打脸。

### ID-DEMO-006 | P2 | ISSUE-334 实体 0/3

- 对抗 **故意** 不透明告警；`source_projection` 有实体。评委问「为什么识别不了主机名」需讲 334 契约，否则像能力缺陷。

### ID-DEMO-007 | 文档 vs 代码

- README / `docs/deployment.md` 对 `make up` vs `up-demo`、Mock vs Live card 的区分 **与代码一致**（ISSUE-350）。旧审计里「文档声称已实现但 CI 契约红」已过时。
- 仍 OPEN 的 tracking EPIC：#643/#632/#615/#610（非本 SHA 回归）。
- #882 BuildKit/Playwright：**SUSPECTED**，本环境未复现。

---

## 9. 维度对照总表

| ID | 维 | 级 | 一句话 |
|---|---|---|---|
| BLK-001 | 1+研判 | P0 | HIGH 非 MEDIUM 超集，漏 `block_domain`，Live 仍 CLOSED |
| BLK-002 | 1+CI | P0 | ruff format 1 文件 |
| BLK-003 | 1+CI | P0 | 前端夹具缺 `simulated` |
| BLK-004 | 1+CI | P0 | ReportAgent caplog 单测 |
| BLK-005 | 1 | P0 | #970 循环导入可复现 |
| BLK-006 | 1+DEMO | P0 | make up-demo observability 卷路径 |
| BLK-007 | 1+5 | P0 | HTTP Mock 未打 scenario → default.json → verifying 卡死 |
| BYP-001 | 2 | 设计 | 328 coverage **故意**不含 domain |
| BYP-002 | 2 | 设计 | 空 LLM → 规则（`LLM_REQUIRED=false`） |
| BYP-003 | 2 | 设计 | auto-investigate 无 response（ISSUE-108/352） |
| BYP-004 | 2 | P2 设计 | ConvergenceGuard fail-open（ISSUE-062） |
| BYP-005 | 2 | P2 | OutputGuard None（测试便利） |
| BYP-006 | 2 | P2 | Memory 非图节点（设计） |
| BYP-008 | 2 | P2 设计 | HTTP 默认 defer response（安全默认） |
| CTR-001 | 3 | 设计 | scorecard PASS+GAP 是 ISSUE-349；GT 在 pytest |
| CTR-002 | 3 | P1 | 358 log 假红（=BLK-004 投影） |
| CTR-003 | 3 | P1 | OpenAPI vs TS simulated（=BLK-003 投影） |
| CTR-006 | 3 | 设计 | AnalysisOnly vs SuperAgent |
| CTR-007 | 3 | P2 | Health 60min 窗口（契约；评测误读） |
| CTR-008 | 3 | P2 | --require-closed 是管道门（ISSUE-301） |
| SEC-001 | 4 | P1 脚枪 | force_local_close admin + bootstrap-token |
| SEC-002 | 4 | P1 | DEV_AUTH 非 prod |
| SEC-004 | 4 | P2 | 租约 202 |
| REL-001 | 5 | 设计 | LLM 降级不挡 CLOSE |
| REL-002 | 5 | P0 | =BLK-007 |
| REL-003 | 5 | P2 | background 丢任务 |
| REL-006 | 5 | 观察 | CI backend-test 墙钟 |
| REL-007 | 5 | P1 认证 | glm 9/9 timeout 不能当研判成功；CLOSE 本身合法 |
| DEMO-001 | 6 | P0 | make up-demo 失败；required HTTP 不 CLOSED |
| DEMO-002 | 6 | P0 | main CI 红 |
| DEMO-003 | 6 | P0 答辩 | Live pytest 红但 CLOSED；scorecard 故意 PASS |

---

## 10. 建议修复顺序（仅方向，本 PR 不改代码；已避开与 main 契约冲突的修法）

1. **遏制完整性（BLK-001）**：HIGH `DATA_EXFILTRATION` 并入 `block_domain`，或 MEDIUM→HIGH lift 改为 **并集**。不要改 328 coverage，不要改 `validate_closed_gate`。
2. **CI 三红（BLK-002–004）**：format、夹具 `simulated`、去掉 caplog 脆断言。
3. **#970（BLK-005）**：拆 `orchestration.__init__` 急切导入。
4. **Docker 金路径（BLK-006/007）**：observability 卷路径；ingest 打 `normalized.scenario`；`disposition is None` 不要叫 `effect_not_ready`；required+none 进 FAILED 而非无限 verifying。**不要**让 required 无写回 CLOSE。
5. **Live 认证（REL-007）**：另开 per-event LLM 成功率门，不要用禁止 CLOSE / 拒绝 `degraded_template` 冒充。
6. 轮换已暴露的 Ark API key。

不要做（与当前 main 测试/ISSUE 冲突）：

- 328 merge 补 `block_domain`（破 `test_has_actionable_includes_domains_but_coverage_needs_ignore_them`）
- scorecard GAP → FAIL（破 ISSUE-349 与 helpers 里 PASS+GAP 断言）
- `--require-closed` 拒绝 `degraded_template`（破 ISSUE-301/212）
- `LLM_REQUIRED=false` 时 0% LLM 禁止 CLOSE（拆掉降级）

---

## 11. 二次代码复核（对照当前 `origin/main` = `6609c79b`）

复核范围：报告全部 ID × 源码/单测/ISSUE 契约 × 仍在库的 Docker 事件行。`git fetch origin main` 后 **main 未领先审计基准**，修这些洞不会和「更新的 main」撞车；会撞的是 **同 SHA 上已合入的 ISSUE 测试**。

### 11.1 遗漏（原报告没写清）

| 遗漏 | 落点 |
|---|---|
| HTTP/Celery 种子 **不写** `normalized.scenario` | Mock 走 ISSUE-199 `default.json`，与对抗 pytest 打 `scenario` 不是一条路。这是 BLK-007 主因 |
| DATA_EXFIL **MEDIUM 整表换成 HIGH** 才丢 `block_domain` | `response_agent.py:809-814`；HIGH 不是超集 |
| `LLM_REQUIRED` 默认 false | 0% LLM 仍 CLOSED / Health 不因 LLM 变红，是开关不是漏做 |
| ISSUE-349 coverage 在 unscored 桶 | scorecard 7/7 PASS+GAP 是契约 |
| required 无 CONFIRMED **不得 CLOSE** | BLK-007 没 CLOSED 符合 ISSUE-302；缺陷是卡死 verifying |

### 11.2 不实（已改正）

| 原表述 | 复核 |
|---|---|
| BLK-007：Risk 没写判定、Mock LLM 失败 | LLM 8/8 success；risk 57 已写；none 是低于 70 分的契约 |
| BLK-007：脚本只批 1 个动作 / 写回 404 是主因 | ticket success + 延迟处置 **approved** 未激活 |
| BLK-001：HIGH 把 block_domain 当低档丢掉 | lift 为补 L3 时替换池，HIGH 漏带 MEDIUM 的 IOC |
| CTR-001：scorecard PASS 是契约漂移 P0 | ISSUE-349 故意；pytest 硬门才是 GT |
| REL-007 / CTR-008：eval OK 证明门禁坏了 | `--require-closed` 只验管道；该场景 GT=`none` |
| 原 P1=14 | 大量与 P0 重复，或把设计写成 bypass |

### 11.3 相互冲突（原报告内部）

| 冲突 | 处理 |
|---|---|
| §2.4 说判定 none 对齐 GT，REL-007 又说 0% LLM 应 fail-closed | 收窄 REL-007 为 **认证卡**，CLOSE 合法 |
| BLK-001 建议改 328 + CLOSED 门，BYP-001 又承认 328 故意不含 domain | 只修 HIGH 规则 |
| BLK-007 听起来像「应该 CLOSED」，同文又说 required 无写回不能 CLOSE | 没 CLOSE 是对的；要修的是卡死与 Mock 剧本 |
| CTR-007/008 当产品 P1，schema/ISSUE-301 已写明行为 | 降为 P2 误读 |

### 11.4 修完是否与当前 main 冲突

当前 `origin/main` 仍是 `6609c79b`。低冲突补丁：BLK-002 format、BLK-003 夹具、BLK-004 改断言、BLK-005 lazy import、BLK-006 compose 路径、HIGH 规则加 `block_domain`（`test_data_exfiltration_high_rules_include_required_tools` 是超集 `in names`）。

**会与 main 已合入测试打架的修法**（已从建议里删除）：改 328 domain merge；scorecard FAIL on GAP；strict 拒 `degraded_template`；无 LLM 禁止 CLOSE。

### 11.5 故意设计，且好处明显大于坏处（不应当缺陷修掉）

| 行为 | 好处 | 若当 bug 修掉的坏处 |
|---|---|---|
| LLM → 规则/模板降级（`LLM_REQUIRED=false`） | LLM 宕机 SOC 不停 | 云厂商超时=全线停摆 |
| ISSUE-328 不含 domain | L3 覆盖可测、不把 IOC 当主机义务 | 与 ISSUE-198 分层被抹平，单测必红 |
| ISSUE-349 scorecard 不含 coverage | 管道认证 vs GT 硬门分离 | helpers 全红；CLOSED 绑研判质量 |
| ISSUE-301 接受 `degraded_template`、not_required 跳过写回 | 管道验收不依赖 glm | 降级报告无法闭环 |
| Health 60min 滚动窗口 | 运维看供应商健康 | 改成 per-event 就不是 health |
| auto-investigate / HTTP 默认不做 response | 避免自动 L3 | 摄取即隔离 |
| ConvergenceGuard 非硬门 | 守卫挂了调查能结束 | 依赖一抖全部 halt |
| force_local_close | 现场逃生 | 可保留，应收紧 bootstrap-token |
| Mock 无 scenario → `default.json` | 生产不硬编码 demo 名 | **demo 种子必须自己打标**；没打标才是洞 |

---

## 附录 A — 动态命令与 CI

```text
# Live AnalysisOnly（通过）
LLM_MODE=openai_compatible LLM_PRIMARY_MODEL=glm-5.2 \
  pytest tests/adversarial/test_agent_adversarial_audit.py -m adversarial_audit
# pipeline finished in 73.4s  PASSED

# Live SuperAgent full_loop（图 CLOSED，pytest 失败）
pytest tests/adversarial/test_agent_adversarial_full_loop.py -m adversarial_audit
# AssertionError: missing=['block_domain×storage-sync-cdn.example']

# 循环导入
cd backend && uv run python -c "import app.services.analysis_only_pipeline"
# ImportError: cannot import name 'run_rag_stage' from partially initialized module ...

# CI
# https://github.com/munos1233/shadowtrace-/actions/runs/32007667156
```

未把 API key、token、完整 raw_payload 写入本文件。

## 附录 C — Docker 补测（2026-08-17）

本 Cloud Agent 镜像起初没有 `docker`。用 sudo 安装 docker.io + compose v2，手动启动 `dockerd`（无 systemd）。overlay2 不可用，退化为 **vfs**。容器间直连 `postgres:5432` **超时**（ICC 坏）；经网桥网关已发布端口（`172.18.0.1:15432/16379/18100`）可达。此 ICC 问题视为 **DinD/嵌套网络限制（SUSPECTED 环境）**，不是业务代码。为跑通 HTTP 闭环使用了 **未提交** 的 compose overlay，不作为产品修复。

| 步骤 | 结果 |
|---|---|
| `docker build` backend/frontend/mock-xdr/worker | **成功**（frontend `pnpm build` 通过；与 CI `tsc` 红不是同一条命令） |
| `make up-demo` | **失败** ID-BLK-006 |
| `make up WORKER=1 SCHEDULER=1` + hairpin DB/Redis/XDR URL | backend/worker **healthy**；`task_mode=celery`；`full_loop_available=true` |
| HTTP Mock 金路径 `insider_data_exfiltration --require-closed` | **FAIL** 240s `verifying`。DB：`insider_threat` / risk 57 / `none` / required；LLM **成功**走 `default.json`（种子无 `normalized.scenario`）。没 CLOSED **符合** required 无写回门；P0 是卡死+剧本脱节（ID-BLK-007 已更正） |
| HTTP Live glm `suspicious_domain_access --require-closed` | **eval OK elapsed_s=433.4 CLOSED**。GT 本就是 `none` + `not_required`（判定 none **对齐**）。处置 `block_domain(brand-new-cdn-example.net)`+ticket 来自 **rule fallback**。报告 `degraded_template`。本事件 LLM **9/9 timeout / 0 token**（ID-REL-007）。当时 Health `success_rate=0.5` 是 60min 窗口混算（ID-CTR-007），本事件成功率 **0%**。`--require-closed` 弱门（ID-CTR-008）。详见 §2.4 |

宿主机端口：Postgres `15432`、Redis `16379`、Backend `18000`、Frontend `13000`、Mock XDR `18100`（避开本机已占用的 5432/6379）。

```text
# 官方 demo（失败）
make up-demo

# 实际跑通的核心栈（需 hairpin overlay，见 /tmp，未入库）
POSTGRES_PORT=15432 REDIS_PORT=16379 BACKEND_PORT=18000 \
  MOCK_XDR_PORT=18100 FRONTEND_PORT=13000 \
  make up WORKER=1 SCHEDULER=1

python3 scripts/dynamic_eval_full_loop.py --seed-via-compose \
  --scenario insider_data_exfiltration --require-closed \
  --base-url http://127.0.0.1:18000 --mock-xdr-url http://172.18.0.1:18100
# → verifying timeout

python3 scripts/dynamic_eval_full_loop.py --seed-via-compose \
  --scenario suspicious_domain_access --seed 7 --require-closed \
  --base-url http://127.0.0.1:18000 --mock-xdr-url http://172.18.0.1:18100 \
  --max-wait-s 600
# → closed 433s（not_required；判定 none=GT；glm 9/9 timeout）
```

## 附录 B — 本轮明确「不是 bug / 不重报」

| 项 | 说明 |
|---|---|
| `--require-closed` 接受 `degraded_template` / not_required 跳过写回 | ISSUE-301/302 管道门，不是 glm 质量门 |
| `suspicious_domain_access` 的 `final_verdict=none` | 场景 GT；不是漏判 |
| Health LLM `success_rate` 60min 窗口 | schema 契约；不是本事件成功率 |
| LLM 降级不挡 CLOSE | `LLM_REQUIRED=false`；规则对齐 GT 时是功能 |
| auto-investigate / HTTP 默认无 response | ISSUE-108/352，避免自动 L3 |
| ISSUE-328 不含 domain | 单测锁定；外泄 domain 靠规则表+对抗 pytest |
| scorecard PASS + coverage GAP | ISSUE-349 unscored；GT 在 `production_checks` |
| ISSUE-312 isolate 不在旧 CLOSED 门 | 已由 `side_effect_convergence` 覆盖；Live 两台主机 isolate 均 effect verified |
| ISSUE-334 文本理解 0 命中 | 不透明告警契约；对抗测试 **断言** hits==0 |
| ISSUE-305 Memory 不进图 | 不可执行步 fail-closed + hook |
| ISSUE-227 Mock simulated CONFIRMED | 非 mock 拒绝 simulated；认证标签 `mock_simulated` |
| Verify already_submitted | 仍评估回执 |
| Outbound 分析正文 | 静态白名单成立 |
| OTEL_ENABLED=false | 遥测 no-op，业务不依赖 |
| 354–362 | 已合入；358 仅测试 log 断言残留 |
| fp_matcher | 已接线，非死代码 |
