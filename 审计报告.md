# ShadowTrace 发布前深度 Bug 探查报告

| 项 | 值 |
|---|---|
| 基准分支 / commit | `main` @ `d524a16f725b6ad43c7088e725b622cd4ca159b5` |
| 探查日期 | 2026-08-16 |
| 立场 | fail-closed；Mock 绿 ≠ Live 研判 |
| 交付范围 | **仅问题与证据，不改产品代码** |
| 探查方式 | 静态（P0 主链路 + 六维）+ 动态（本机 Postgres 16/pgvector + Redis 7 + 两条 **Agent 全链路**） |

## 执行摘要

**结论：闭环管道在 Mock 对抗场景上已能 CLOSED 且隔离 WKS+DB（ISSUE-328 合入后）；发布仍被 CI 两处红挡住；glm 研判本轮未能跑。**

按你的要求，Live LLM 应来自桌面 **`/Users/apple/Desktop/shadowtrace-test-local/.env.live`**（`LLM_MODE=openai_compatible` / glm-5.2，**不是 MockGPT**）。**本云 Agent 工作区是 `/workspace`，该路径不存在、未挂载**，进程里也没有 `LLM_API_KEY`。因此两条全链路只能用对抗 golden + `LLM_MODE=mock` 验证 **Agent 图与 328 覆盖**，**不能**当作 glm 研判质量。把 Mock PASS 说成自主研判 = 假成功。

| 级别 | 本轮仍成立 |
|------|------------|
| P0 发布阻断 | CI `backend-evaluation` detection `fail_closed`；`backend-system-test` 并发租约丢失 |
| P1 研判缺口 | **Live glm 未跑**（配置文件不可达）；理解维 0/3（334 合同，糊标题） |
| 已合入不再当未修 | ISSUE-328（#959 **CLOSED**）、ISSUE-350（job 名 `backend-closure-gates-mock`）、335–349、351–353 多数 |
| SUSPECTED | Docker compose 金路径（本环境无 Docker daemon）；#970 循环导入未打爆 |

---

## 1. 元信息

### 1.1 Live 配置（按你的指示查找）

| 查找 | 结果 |
|------|------|
| `/Users/apple/Desktop/shadowtrace-test-local/.env.live` | **No such file**（云 VM 无 `/Users/apple`） |
| 仓库内 `.env.live` | 无（仅 `infra/.env.live.example`，密钥为空） |
| 环境变量 `LLM_API_KEY` / `LLM_API_BASE_URL` | **MISSING** |
| `LLM_MODE` | 默认/`mock` |

仓库内历史文档 [`深度问题调查报告-live-full-loop-20260811.md`](深度问题调查报告-live-full-loop-20260811.md) 也写测试目录是上述 Desktop 路径。要补 glm 研判：把 `.env.live` **拷到本工作区但不 commit**，或把 `LLM_*` 注入 Agent 环境后重跑同一对 pytest。

### 1.2 近期 main

- #1016 **ISSUE-328**：containment 按 EntitySet 覆盖 merge；DATA_EXFIL HIGH/CRITICAL 含 `isolate_host`；`must_response_targets_gated=[]`
- #1017 **ISSUE-350**：Checks 名 `backend-closure-gates-mock`；产物 `scorecard_header.certification_card=mock_plumbing`

### 1.3 亲自跑的两条 Agent 全链路（Mock plumbing，非 glm）

```bash
export DATABASE_URL=postgresql+asyncpg://shadowtrace:shadowtrace@localhost:5432/shadowtrace
export REDIS_URL=redis://localhost:6379/0
export LLM_MODE=mock ALLOW_LIVE_SIDE_EFFECTS=false DISPOSITION_MODE=mock_xdr
cd backend
uv run --frozen python -m pytest tests/adversarial/test_agent_adversarial_audit.py \
  -m adversarial_audit -v -s -o addopts=
uv run --frozen python -m pytest tests/adversarial/test_agent_adversarial_full_loop.py \
  -m adversarial_audit -v -s -o addopts=
```

场景：`adversarial_credential_db_staging_exfil`（糊标题、高噪声）。产物 gitignore：`backend/tests/adversarial/artifacts/latest_*.json`。

### 1.4 分数卡

| 维 | AnalysisOnly（27.4s） | SuperAgent full_loop（~38s，图 12.8s） |
|----|----------------------|----------------------------------------|
| pytest | PASSED | PASSED |
| 终态 | REPORTING | **CLOSED** |
| type / risk / verdict | `data_exfiltration` / 76 / `confirmed_threat` | 同左 |
| text_understanding | **0/3 实体、0/2 IOC** | 同左 |
| source_projection | 3/3 + 2/2（含上传域） | 同左 |
| 遏制 | 分析路径无 response（coverage GAP 含 DB） | **`isolate_host(WKS-DATA-031)` + `isolate_host(SRV-DB-STG-02)`** + disable 账号 + block 外网 IP；`disposition_target_gaps=[]`；`strict_aligned=true` |
| 写回 | n/a | `certification=mock_simulated`；页眉 `mock_plumbing` |
| 计分 | 人类 PASS + understanding 0 | 7/7 PASS + understanding 0 |

**相对 `74ffbbc`：** 328 合入后 Mock 路径 **补上了 DB 隔离**（此前 `isolate_host×SRV-DB-STG-02` gated/缺口）。这仍是 golden/覆盖门，不是 glm。

---

## 2. 维度 1 — Blocker

### ID-BLK-001 | 已关闭 | ISSUE-328 任意遏制即放行

- **位置（现码）**：[`response_plan_quality_gate.py:10-22,444-515`](backend/app/agents/rules/response_plan_quality_gate.py) `_merge_entity_coverage`；不再 `if any containment: return`。
- **规则**：[`default_response_rules.py:113-127`](backend/app/agents/rules/default_response_rules.py) DATA_EXFIL HIGH/CRITICAL 含 `isolate_host`。
- **证据**：本环 full_loop 同时隔离 WKS 与 DB；#959 **CLOSED**。
- **残留**：Live glm 规则回退/LLM 路径未在本环境复跑。禁止把 Mock 双 isolate 当成 glm 能力。

### ID-BLK-002 | P0（CI） | `backend-evaluation` detection `fail_closed`

- **位置**：CI run `31917018845`；detection 切片 `pass_rate=0.666…`，`gate_verdict=fail_closed`（Redis 已在，不是 337）。
- **修复**：查 unevaluable 用例。禁止关 `required_gate`。

### ID-BLK-003 | P0（CI） | system 并发租约丢失

- **位置**：`tests/system/test_concurrency_smoke.py::test_ten_concurrent_events_reach_terminal_state_without_cross_talk`
- **现象**：`InvestigationLeaseLostError`（run `31917018845`）。本环境未复跑 10 并发。
- **修复**：锁顺序/死锁重试；单事件 fail-closed 不应打翻整组。

### ID-BLK-004 | 已排除 | required 无 CONFIRMED 即 CLOSED

[`workflow.py`](backend/app/models/workflow.py) `validate_closed_gate`：非 mock 拒 simulated/弱 evidence；mock 宽门 + 认证标签 `mock_simulated`（351）。`disposition_is_mock` 默认 False（345）；模式 allowlist `mock_xdr`（344）。

### ID-BLK-005 | 已排除 | Verify `already_submitted` 跳过两阶段

本环 `verify_agent_ran=true`，走到 CLOSED。

### ID-BLK-006 | lint format | **已排除（本 commit）**

本机 `ruff check` / `ruff format --check` 全绿；CI `backend-lint` **success**。

---

## 3. 维度 2 — Bypass / No-op

### ID-BYP-001 | 328 覆盖门 | **已接入**（见 BLK-001）

### ID-BYP-002 | Memory/Storyline 非图节点

ISSUE-305 有意。`GRAPH_EXECUTABLE_AGENTS` 由 P0 节点派生。不要塞进 LangGraph。

### ID-BYP-003 | Trajectory 基础设施失败 | **503**（ISSUE-341）

[`trajectory.py`](backend/app/api/v1/trajectory.py) `DependencyUnavailableError`。空轨迹仍 200 + `insufficient_trace`。

### ID-BYP-004 | bootstrap-demo | **已命名分流**（ISSUE-352）

`bootstrap-demo-analysis` ≠ CLOSED；`bootstrap-demo-full-loop` / `demo-full-loop` 才是金路径。本环境无 Docker，compose 未跑。

### ID-BYP-005–006 | playbook 失败跳 LLM / RFC1918 block | **已修（340/339）**

本环 block 目标为 `198.51.100.44` / `198.51.100.77`（外网），未见 RFC1918。

---

## 4. 维度 3 — 契约 / 测试假象

### ID-CTR-001 | Mock CI ≠ Live 研判 | **披露已落地，缺口仍在**

- Checks 名 **`backend-closure-gates-mock`**；`LLM_MODE: mock` 钉死（[`ci.yml:84-121`](.github/workflows/ci.yml)）。
- 产物页眉：`certification_card=mock_plumbing`，「PASS 不是 Live reasoning」。
- **风险**：本轮 **没有 Live 卡**。ISSUE-350 修的是命名，不是 glm 评测。

### ID-CTR-002 | 人类 PASS 与理解 0 并存

334 合同：糊标题无实体名 → `text_understanding` 0；source/echo 有三实体。349 旁注已出现。计分仍不含理解维。

### ID-CTR-003 | DB 隔离 gated | **已解**

`must_response_targets_gated: []`。本环 `strict_aligned=true`。

### ID-CTR-004 | artifact pending | **已 overlay**（342）；本环 status=success

### ID-CTR-005 | contract-drift | 本 run **success**

### ID-CTR-006 | DNS 输入 | **338 已修**；本环无 query_dns skip-failed

### ID-CTR-007 | 零 xfail；skipif 绑 PG/Redis

---

## 5. 维度 4 — 安全

### ID-SEC-001 | DEV_AUTH_TOKENS 生产拒绝 | fail-closed，非 bug

### ID-SEC-002 | 分析内容出站 | 未找到：Command `extra=forbid` + guard allowlist

### ID-SEC-003 | raw_payload 明文 | **SUSPECTED**（未做密码字段抓包）；GET 不回 raw

### ID-SEC-004 | force_local_close | admin API+服务层（308）；跳过写回、标 `external_unsynced`

### ID-SEC-005 | Mock 冒充 live | `ALLOW_XDR_WRITEBACK=false`；认证 `mock_simulated`

### ID-SEC-006 | Verify vs CLOSED 档位（live）| P2：Verify 主要降 ACK；CLOSED 非 mock 还拒 `status_queried`。本 Mock 环未打中。

---

## 6. 维度 5 — 可靠性

### ID-REL-001 | 并发冒烟租约丢失 | 同 BLK-003

### ID-REL-002 | degraded_flags | 353：bootstrap 走 factory；Redis 自愈已接线

### ID-REL-003 | SoftTimeLimit 调度 | 324 已合；本审计未打

### ID-REL-004–005 | mock 默认/子串 | 345/344 已修

---

## 7. 维度 6 — DX / 答辩

### ID-DEMO-001 | 官方金路径需 Docker | 本环境 daemon 不可用，**未** `make up-demo`

### ID-DEMO-002 | main CI 非全绿

run `31917018845`（`d524a16`）：

| Job | 结论 |
|-----|------|
| backend-lint / closure-gates-**mock** / contract-drift / regression / frontend-build / docker-build | success |
| backend-evaluation | **failure**（detection fail_closed） |
| backend-system-test | **failure**（并发租约） |
| backend-test | cancelled（上游失败） |

### ID-DEMO-003 | 答辩禁止句

「对抗 pytest 绿所以 glm 会研判 / 会隔离 DB」——本轮 DB 隔离只在 **Mock golden+328 门** 上证明。

### ID-DEMO-004 | 双 severity | 330 按设计（对外 high / 分诊 medium）

### ID-DEMO-005 | #970 循环导入 | P1 SUSPECTED：`workflow_graph` import `run_rag_stage`；analysis_only 对 super_agent lazy import。本环加载成功。

---

## 8. 发布建议

**需改后发（CI）+ 补 Live 研判卡才能声称研判质量。**

1. 修 detection `fail_closed` 与并发租约丢失。
2. 把 `shadowtrace-test-local/.env.live` 提供给本环境（勿 commit），重跑：
   - `test_agent_adversarial_audit.py`
   - `test_agent_adversarial_full_loop.py`
   超时 `ADVERSARIAL_FULL_LOOP_TIMEOUT_S=600`，`LLM_MODE=openai_compatible`。
3. Live 分数卡对照：理解维、是否仍双 isolate、是否 rule fallback、写回是否仍 `mock_simulated`。

### 不要做的

- 不要把本次 Mock 12.8s CLOSED + 双 isolate 写成 glm 研判。
- 不要把 isolate 塞进 `validate_closed_gate`。
- 不要关 detection `required_gate`。
- 不要恢复 CoT。

---

## 9. 证据索引（无密钥）

| 项 | 位置 |
|----|------|
| AnalysisOnly / full_loop 产物 | `backend/tests/adversarial/artifacts/latest_audit.json`、`latest_full_loop_audit.json` |
| CI | https://github.com/munos1233/shadowtrace-/actions/runs/31917018845 |
| 328 | https://github.com/munos1233/shadowtrace-/issues/959 （CLOSED） |
| 350 | job `backend-closure-gates-mock` |
| 缺的 Live 配置 | `/Users/apple/Desktop/shadowtrace-test-local/.env.live`（仅用户本机） |
