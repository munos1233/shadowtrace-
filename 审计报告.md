# 预发布全链路 Debug 分级清单

| 项 | 值 |
|---|---|
| 检出分支 / commit（探查起点） | `cursor/pre-release-audit-a07a` @ `48f191a2`，随后合入本轮修复 |
| 探查日期 | 2026-08-26 |
| 立场 | fail-closed；Mock CLOSED ≠ Live 研判；冻结项不当成要涂绿的洞 |
| **不含** | PR #1065（storyline 4096、ReportAgent 继承 90s timeout、timeline journal overlay） |

冻结（不当成「修掉才算过」）：两张认证卡不合并；ISSUE-328 域不进 coverage；`--require-closed` 接受 `degraded_template`、拒绝 `incomplete_placeholder`；金标禁止手搓 `POST /events`、禁止靠 `APPROVAL_TIMEOUT` 收尾；写回恢复不得消耗 `replan_count`。

---

## 执行摘要

官方 Mock 金标 **`EVAL_REQUIRE_CLOSED=1 EVAL_SCENARIO=insider_data_exfiltration make eval-full-loop` 未 CLOSED**：脚本批准后事件停在 `verifying`，Celery 已空闲，`execution_substate=manual_resolution`。根因是 `final_verdict=none` 时 Phase 2 无法派生终端 disposition，`blocked_writebacks=['act-b4a99e35']`。这会卡住 **plumbing 金标 CLOSED**，不是 Live 研判卡。

本轮**已修且实证**的产品洞（不是涂测试）：

1. `make up-demo` 合并观测 compose 时裸 `./prometheus.yml` 解析到 `infra/`，runc `not a directory`。
2. `scheduler-beat` healthcheck 调用 slim 镜像没有的 `pgrep`，进程在跑却 unhealthy。
3. `KnowledgeStore.upsert_chunks` 一次 embed 超过 `EMBEDDING_MAX_BATCH_SIZE=64`（官方 ATT&CK 78 条），`LOAD_KB=true` / `python -m scripts.load_attack_kb` 失败。

动态重规划 vs 写回恢复边界与 RAG 主路径 pin **单测通过**。`AttackKBService.search_techniques` 的 #644 旁路仍在，但 **不在图内 RAG**，禁止写成「检索完全坏了」。

本环境无 `.env.live`：**Live 研判卡未证伪**，不是 blocker。

| 级别 | 本轮条目 |
|------|----------|
| blocker（会误伤 Mock 金标 CLOSED） | 官方 insider 金标停在 verifying / manual_resolution（`final_verdict=none`） |
| 已修（产品） | compose 观测绑路径；beat `pgrep`；KB upsert 分批 embed |
| 疑似 | Socket.IO health degraded（schema `_WrappedReferencingError`）；RAG production `Settings()` 单测在本分支未跟 main 的 `production_settings()` |
| 已知冻结 | ISSUE-328；#644 直连 hybrid/vector；ReportAgent 30s / timeline snapshot 摘要（#1065 才改，本分支 Mock GET `/timeline` **200**） |
| 环境限制 | 无 systemd，dockerd 需 vfs；容器互通需 `bridge-nf-call-iptables=0`；无 LLM key |

---

## 1. 怎么跑的

```bash
# Docker：本环境无 systemd，vfs storage + sysctl net.bridge.bridge-nf-call-iptables=0
make down-demo && make down-v && make up-demo
EVAL_REQUIRE_CLOSED=1 EVAL_SCENARIO=insider_data_exfiltration make eval-full-loop
```

矩阵 `make eval-full-loop-matrix`（`--profile-by-scenario`）**未再跑三场景**：insider 已在同一栈上以相同 Verify→manual_resolution 失败，再刷 3×240s 不会增加信息。

无 compose 单测包（编排/RAG 单元/Verify/eval-guard）：**335 passed**（去掉 production Settings 夹具与需 Postgres 的项）。Postgres 起来后 playbook/case/attack_kb、release pin、workflow_graph：**140 passed**。

---

## 2. 分级问题清单

每条：**级别** · **复现** · **路径** · **会不会误伤金标 CLOSED**。

### B-1 | 官方 Mock 金标停在 verifying（final_verdict=none）

- **级别：** blocker（plumbing `--require-closed`）
- **复现：** `EVAL_REQUIRE_CLOSED=1 EVAL_SCENARIO=insider_data_exfiltration make eval-full-loop`
- **证据：** `evt-20240615-420918b5`；轨迹 `new → … → waiting_approval → verifying`；批准后 `create_ticket=success`；`verify_agent` output `overall_status=manual_resolution`，`blocked_writebacks=['act-b4a99e35']`，`need_action_replan=false`；`final_verdict=none`；Celery `inspect active` 空。
- **路径：** `backend/app/services/terminal_disposition_resolver.py`（非 FP / 非 `confirmed_threat` → `need_manual_resolution`）；`backend/app/agents/verify_agent.py` Phase 2 activation skipped → `manual_hold_node`；`backend/app/orchestration/workflow_graph.py` `manual_hold_node` 置 `halted=True` 且 **不改 EventStatus 出 VERIFYING**。
- **会不会误伤金标 CLOSED：** **会。** 评测在 240s 墙钟时报 `finals={…: 'verifying'}`。不是靠拉长 `APPROVAL_TIMEOUT` 能收尾的（批准早已完成）。
- **本轮未改：** 不改 LangGraph / CLOSED / Outbox 合同去换绿。Mock 计划只有 `create_ticket` + deferred disposition，风险分 57、`verdict=none`，与 ISSUE-328 遏制缺口是不同轴。

### F-1 | `make up-demo` 观测栈 bind-mount（已修）

- **级别：** blocker（demo 起不来）→ **已修**
- **复现（修前）：** 合并 `-f infra/docker-compose.yml -f infra/observability/docker-compose.observability.yml` 时 `./prometheus.yml` 落到 `infra/prometheus.yml`（目录占位），runc `not a directory`。otel 同理。
- **路径：** `infra/observability/docker-compose.observability.yml`；Makefile `COMPOSE_DEMO` / `up-observability` 现传 `OBSERVABILITY_CONFIG_DIR`
- **测试：** `backend/tests/test_infra/test_compose_observability_binds.py`
- **会不会误伤金标 CLOSED：** 修前 **会**（栈起不来）。修后观测容器可起。

### F-2 | scheduler-beat healthcheck 依赖 `pgrep`（已修）

- **级别：** 疑似（beat 实际在跑，health 红）→ **已修**
- **复现（修前）：** `docker inspect …scheduler-beat` → `/bin/sh: 1: pgrep: not found`
- **路径：** `infra/docker-compose.yml` `scheduler-beat.healthcheck`；现读 `/proc/1/cmdline` 含 `beat`
- **测试：** `test_scheduler_beat_healthcheck_does_not_require_pgrep`
- **会不会误伤金标 CLOSED：** 直接金标不依赖 beat health；会误伤 `smoke-demo` / 任何 `depends_on: service_healthy` 链。

### F-3 | attack_kb 加载超过 embed batch（已修）

- **级别：** blocker（`LOAD_KB=true`）→ **已修**
- **复现（修前）：** `cd backend && python -m scripts.load_attack_kb` → `EmbeddingCompatibilityError: embedding batch exceeds configured limit`（78 > 64）
- **路径：** `backend/app/services/knowledge_store.py` `upsert_chunks`；`backend/app/core/embedding/service.py` `max_batch_size`
- **测试：** `test_upsert_chunks_batches_above_embed_limit`
- **会不会误伤金标 CLOSED：** 默认金标不 `LOAD_KB`，**不直接**挡 CLOSED；挡知识库加压与 `make bootstrap LOAD_KB=true`。修后 Loaded 78，`GET /knowledge` total=81。

### S-1 | RAG `app_env=production` 单测在本分支构造 Settings 失败

- **级别：** 疑似 / 测试卫生（本分支落后 main）
- **复现：** `pytest tests/test_rag/test_context.py::test_from_rag_input_requires_tenant_in_production` → `task_mode=background: volatile execution forbidden in production`
- **路径：** `backend/tests/test_rag/test_context.py`、`test_pipeline.py`；main 已用 `production_settings()`
- **会不会误伤金标 CLOSED：** **不会。** 产品 fail-closed 在工作；夹具未带齐 production 安全字段。不在本分支回港 main 的测试 helper（避免和审计范围搅在一起）。

### S-2 | Socket.IO health `degraded`

- **级别：** 疑似
- **复现：** `GET /api/v1/health` → `socketio.status=degraded`，`last_error_class=_WrappedReferencingError`，`consecutive_failures=1`
- **路径：** health 的 socketio 订阅；合约/schema 引用
- **会不会误伤金标 CLOSED：** 金标未因 Socket.IO 失败；整体 health 为 `degraded`。未当 blocker。

### K-1 | RAG #644 直连旁路（已知，未当「检索全坏」）

- **级别：** 已知冻结 / 文档化缺口
- **实证：** 图内 `rag_node` → `RAGAgent` → `RetrievalPipeline`（rewrite → plan validate → hybrid → RRF → rerank）；`attack_kb`/`playbook_kb` release-pin。`AttackKBService.search_techniques` 在 semantic 下直接 `vector_search_query`（无 tenant / KnowledgeQueryPlan），**生产调用方仅测试与 loader**，不在 SuperAgent 图。`case_kb_service` / `playbook_kb_service.search` 仍 `hybrid_search`。
- **路径：** `backend/app/rag/pipeline.py`；`backend/app/services/attack_kb_service.py`；`backend/app/services/knowledge_store.py`
- **复现：** `pytest tests/test_rag/test_release_pinning.py tests/test_services/test_knowledge_query_plan_storage.py`（pin + 跨租户过滤在 **pipeline/store 带 plan 的路径** 通过）
- **会不会误伤金标 CLOSED：** **不会**（金标不依赖 semantic `search_techniques`）。禁止写成检索完全无结果。

### K-2 | 动态重规划 vs 写回恢复（合同成立）

- **级别：** 已知冻结（写回不得吃 `replan_count`）— 静态 + 单测 **符合合同**
- **路径：** `replan_handler.py` `MAX_REPLAN_COUNT=3`，超限 escalate **不**再 +1；`writeback_recovery_handler.py` 所有 patch 置 `verify_need_action_replan=False`，注释禁止进 REPLANNING。`route_after_writeback_recovery` 仍 **映射** `ROUTE_REPLAN`（若脏 flag 残留），但 handler 出口清零。
- **复现：** `pytest tests/test_orchestration/test_replan_loop.py tests/test_orchestration/test_writeback_recovery.py tests/test_orchestration/test_graph_resume.py`
- **金标缺口：** `eval-full-loop` **没有**强制一次真实 replan（本事件 `replan_count=0`）。加压靠上述 pytest，未改金标夹具。
- **会不会误伤金标 CLOSED：** 本事件未走 replan；写回恢复也未消耗 replan 预算。

### K-3 | FP 全环不短路（ISSUE-114）

- **级别：** 已知设计
- **路径：** `workflow_graph.py` `route_after_fp_adjudication` 恒 `ROUTE_CONTINUE`
- **会不会误伤金标 CLOSED：** 短闭环只允许 `--analysis-only`；矩阵才需要 `--profile-by-scenario`（本次矩阵未跑，见上）。

### K-4 | ReportAgent 30s / timeline snapshot 摘要（#1065，本分支）

- **级别：** 已知冻结（相对 #1065）；**Mock 金标不是 blocker**
- **路径：** `backend/app/agents/report_agent.py` `LLM_TIMEOUT_SECONDS = 30.0`；`backend/app/api/v1/timeline.py` `AttackStoryline.model_validate(storyline)`
- **实证（本分支 Mock）：** `GET /api/v1/events/evt-20240615-420918b5/timeline` **HTTP 200**，完整 phases + claim_refs，`generated_by=rule`。API snapshot 里的 `storyline` 是 summary（`phase_count`），**没有**被当成 timeline 正文。Live 截断 JSON → 500 的洞在本环境 **未证伪**（无 glm key）。
- **会不会误伤金标 CLOSED：** Mock 规则故事线不误伤；Live 卡另计。

### K-5 | ISSUE-328

- **级别：** 已知冻结
- **说明：** adversarial PASS+GAP，containment 覆盖不是本次涂绿对象。本金标 mock 计划甚至没有 `isolate_host`（只有 `create_ticket`）。

### E-1 | 本云环境

- **级别：** 环境限制
- **Docker：** 无 systemd；overlay2 不可用，vfs 可用；`bridge-nf-call-iptables=1` 时容器互访 TCP 超时（backend alembic TimeoutError）。本机处理：`sysctl net.bridge.bridge-nf-call-iptables=0`。不是产品合同。
- **Live：** 无 `.env.live`，未跑 `make up-live-reasoning`。**不得把本次 Mock 当 glm 研判证明。**

---

## 3. 隐秘路径结论（用户点名）

| 路径 | 结论 |
|------|------|
| LangGraph 动态重规划 | 单测覆盖 evaluate / 超限 escalate / resume / `route_after_replan`。金标未触发。 |
| 写回恢复 vs replan | handler 不增量 `replan_count`，不进 REPLANNING。 |
| 知识库 / RAG pin | Pipeline + release pin 单测绿；playbook `health.playbook_resources.active_release_id=krel-89eac7fdd0b13977`；attack_kb 分批修复后 78 条可检索。#644 旁路仍在 service 层。 |
| GET `/timeline` | 本 Mock 事件 200，不是 snapshot summary 500。 |

---

## 4. 明确不做

- 不把 PR #1065 未合并改动算进本分支「已修复」。
- 不改 LangGraph / CLOSED / Outbox 合同，让 `final_verdict=none` 的 insider 金标涂绿。
- 不写绑死单一 `event_id` 的过拟合测试。
- 无 LLM key 时不假装跑过 Live 卡。
