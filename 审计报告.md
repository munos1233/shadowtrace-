# ShadowTrace 发布前深度 Bug 探查报告

| 项 | 值 |
|---|---|
| 基准分支 / commit | `main` @ `74ffbbc210cf83828e4f23e531e14610f5beee6d` |
| 探查日期 | 2026-08-15 |
| 立场 | fail-closed；无证据不臆测「已实现」；Mock 绿 ≠ Live 研判 |
| 交付范围 | **仅问题与证据，不改产品代码** |
| 探查方式 | 静态（P0 主链路 + 六维 + 对照 ISSUE-335–353）+ 动态（本机 Postgres 16/pgvector + Redis 7 + 两条 **Agent 全链路**） |

## 执行摘要

**结论：需改后发。** Mock 管道与对抗 golden 闭环能跑通，但（1）**ISSUE-328 仍 OPEN**：containment 任意遏制即放行 + DATA_EXFIL 规则无 `isolate_host`，本环 Mock 漏隔离 `SRV-DB-STG-02`；（2）**研判理解维 0/3**（糊标题诚实，附录回声不算理解）；（3）**main CI 非全绿**（lint format 漂移、detection evaluation `fail_closed`、system 并发死锁）。

本环境 **没有 Docker daemon、没有 `LLM_API_KEY`**，无法复现上一轮 Live glm-5.2。按 fail-closed：**不得把本次 Mock PASS 写成 glm 研判证明**。两条全链路仍调用真实 Agent 图（Triage→…→Report / SuperAgent→Approve→Execute→Verify→CLOSED），LLM 走对抗 golden pack。

| 级别 | 数量（去重后，本轮仍成立） |
|------|----------------|
| P0（发布阻断 / 遏制覆盖） | 2（ISSUE-328；CI 非全绿） |
| P1 Should-Fix | 5 |
| P2 / 有意设计 | 4 |
| 已合入不再当未修 | ISSUE-335–349、351–353 多数 |
| SUSPECTED | 1（#970 循环导入未在本环境打爆） |

---

## 1. 元信息与探查范围

### 1.1 环境

| 依赖 | 本云环境 | 处理 |
|------|----------|------|
| Docker daemon | **不可用** | 未跑 `make up-demo` |
| Postgres / Redis | apt 安装 16+pgvector / Redis 7，本机 `:5432` / `:6379` | 对抗 pytest 用 `DATABASE_URL` / `REDIS_URL` |
| Live glm（`.env.live`） | **不存在**；进程环境无 `LLM_*` | 两条全链路 `LLM_MODE=mock` |
| 本地 lint | `ruff check app tests` **All checks passed** | CI `ruff format --check` 另红 |

### 1.2 近期 main

`git log -15` 以 ISSUE-337/352/353/351/349 合入为主。对照上一轮（`2c55c81` / `738c478`）：大量质量债已落地，**不要回潮当成未修**。

仍 OPEN 且与本审计相关：

- [#959 ISSUE-328](https://github.com/munos1233/shadowtrace-/issues/959) P0 containment 覆盖
- [#994 ISSUE-350](https://github.com/munos1233/shadowtrace-/issues/994) Mock CI ≠ Live 研判
- [#970](https://github.com/munos1233/shadowtrace-/issues/970) `analysis_only_pipeline` ↔ `workflow_graph` 循环导入

### 1.3 P0 主链路（已对照代码）

```
Ingestion → EventService → investigate fork
  ├─ graph: SuperAgent / workflow_graph
  │    triage→planner→evidence→fp→[rag?]→graph→risk→response→approval→execute→verify→report→close
  └─ analysis_only: AnalysisOnlyPipeline（停 REPORTING）
StateMachine + validate_closed_gate + side_effect_convergence
ActionExecution → DispositionSync → VerifyAgent → close
```

### 1.4 明确不重开的合同

- 不把 `isolate_host` 塞进 `validate_closed_gate`（ISSUE-312）
- 不把 Memory/Storyline 塞进 LangGraph（ISSUE-305）
- 不恢复 CoT `reasoning`
- 不把 Mock SUCCESS 写成 live `readback_verified`（ISSUE-351 已改认证语言）

---

## 2. 研判质量主证据（亲自跑的两条 Agent 全链路）

场景：`adversarial_credential_db_staging_exfil`（糊标题、高噪声、ground truth：`svc-analytics-47` / `WKS-DATA-031` / `SRV-DB-STG-02` / VPN `198.51.100.44` / 上传域 `storage-sync-cdn.example`）。

```bash
export DATABASE_URL=postgresql+asyncpg://shadowtrace:shadowtrace@localhost:5432/shadowtrace
export REDIS_URL=redis://localhost:6379/0
export LLM_MODE=mock ALLOW_LIVE_SIDE_EFFECTS=false DISPOSITION_MODE=mock_xdr
cd backend
uv run --frozen python -m pytest tests/adversarial/test_agent_adversarial_audit.py \
  -m adversarial_audit -v -s -o addopts=
uv run --frozen python -m pytest tests/adversarial/test_agent_adversarial_full_loop.py \
  -m adversarial_audit -v -s -o addopts=
```

产物：`backend/tests/adversarial/artifacts/latest_audit.json`、`latest_full_loop_audit.json`。

### 2.1 分数卡

| 维 | AnalysisOnly（28.86s，管线 5.5s） | SuperAgent full_loop（34.86s，图 11.9s） |
|----|-----------------------------------|----------------------------------------|
| pytest | **PASSED** | **PASSED** |
| 终态 | `REPORTING` | **`CLOSED`** |
| event_type / risk / verdict | `data_exfiltration` / 76 / `confirmed_threat` | 同左 |
| 对外 severity / 分诊 | high / medium（ISSUE-330 双源，按设计） | 同左 |
| text_understanding | 实体 **0/3**，IOC **0/2** | 同左 |
| source_projection | 实体 3/3，IOC 2/2（含上传域） | 同左 |
| echo_only | 与 source 相同（报告/摘要回声） | 同左 |
| DNS | `query_dns` skip 空；采集 `completed` | 同左 |
| 遏制 | 分析路径无 response（coverage GAP 旁注账号/WKS/VPN） | `disable_account(svc-analytics-47)` + `isolate_host(WKS-DATA-031)` + `block_ip(198.51.100.44)`；**缺 `isolate_host×SRV-DB-STG-02`**（gated） |
| 计划来源 | n/a | `generated_by=llm`（golden），strategy：Disable account, isolate staging workstation, block VPN |
| 写回认证 | n/a | `simulated=true`，产物 `confirmation_evidence=readback_verified`，**certification=`mock_simulated`**（ISSUE-351） |
| 人类 verdict | PASS + coverage GAP + understanding 0 | PASS + understanding 0 + `writeback=simulated` |
| 计分 | 5/5（不计理解/覆盖） | 7/7；`disposition_targets_aligned=true`（WKS 在）；`strict_aligned=false`（DB） |
| report_quality | `incomplete_placeholder`（不挡关单） | 同左 |
| OutputQuality（未计分） | triage **0.565 warn**（grounding 0.30 / specificity 0.00） | 同左 |

人类 PASS 旁注覆盖/理解（ISSUE-349）**已生效**。计分仍不含理解维——PASS 与 0/3 并存是合同，不是漏测。

### 2.2 研判一句话

Mock golden **会**隔离 WKS、禁用账号、封 VPN 源，事件能 CLOSED；**不会**隔离 DB 主机；**不会**从糊标题读出三实体（334 诚实）。这证明 **管道 + 剧本**，不证明 glm 在未见剧本时的研判。上一轮 Live glm 规则回退 **零 isolate**——该失败模式本环境未能复跑。

### 2.3 与 ISSUE-328 的关系

```
apply_containment_quality_gate
  if _containment_candidates(candidates): return   # 任意 CONTAINMENT_TOOLS
DATA_EXFIL HIGH/CRITICAL 规则：disable_account + block_ip，无 isolate_host
Mock golden：有 isolate WKS，无 isolate DB
```

本环 golden 已有 isolate+disable+block → gate **early-return**，不会补 DB。`must_response_targets_gated` 含 DB，未设 `ADVERSARIAL_STRICT_DISPOSITION_TARGETS` 时 pytest 不因缺 DB 失败。

---

## 3. 维度 1 — 重大代码硬性 Blocker

### ID-BLK-001 | P0 | ISSUE-328 containment 任意遏制即放行

- **位置**：[`response_plan_quality_gate.py:26-34,275-276`](backend/app/agents/rules/response_plan_quality_gate.py)；[`default_response_rules.py:103-122`](backend/app/agents/rules/default_response_rules.py)
- **现象**：`CONTAINMENT_TOOLS` 含 `block_ip`/`disable_account`。已有任一即 return。DATA_EXFIL HIGH/CRITICAL **无** `isolate_host`。
- **本环**：Mock golden 隔离 WKS，**漏 DB**（`disposition_target_gaps_strict_pending=["isolate_host×SRV-DB-STG-02"]`）。Live glm 路径（历史）规则回退可零 isolate。
- **证据**：本机 full_loop 产物；Issue **#959 OPEN**。
- **修复**：按实体类覆盖 merge（账号→disable、主机→isolate、外网 IP→block）；规则表补 isolate；定向只扩当前 EntitySet，禁止盲隔诱饵。禁止只加规则当修好；禁止 isolate 进 `validate_closed_gate`。

### ID-BLK-002 | P0（CI） | main `backend-lint`：`ruff format --check` 5 文件漂移

- **位置**：CI run `31881813666` job `backend-lint`；本机复现 `5 files would be reformatted`
- **文件**：`tests/test_agents/test_triage_prompt.py` 等 5 个测试（listcomp 折行）
- **证据**：本机 `ruff check` 绿、`ruff format --check` 红；CI 因 format 退出码 1。
- **修复**：`ruff format` 这 5 个测试文件。禁止放宽 format 门禁。

### ID-BLK-003 | P0（CI） | `backend-evaluation` detection `fail_closed`

- **位置**：`.github/workflows/ci.yml` `backend-evaluation`；日志 `detection_ci_run.json`：`pass_rate=0.666…`，`gate_verdict=fail_closed`，`unevaluable_coverage=1.0`
- **现象**：ISSUE-337 Redis **已加且 healthy**。本红是 detection 评测门禁诚实失败，不是缺 Redis。
- **修复**：对照 detection shadow 基线/用例为何 unevaluable；**禁止**把 `required_gate` 改 false 去对齐 demo。

### ID-BLK-004 | P1（CI / 可靠性） | system 并发冒烟死锁 + 租约丢失

- **位置**：`tests/system/test_concurrency_smoke.py::test_ten_concurrent_events_reach_terminal_state_without_cross_talk`
- **现象**：`DeadlockDetectedError` 后 `InvestigationLeaseLostError`。同 job 其它 degradation 用例 PASSED。
- **证据**：CI run `31881813666` system-test failure。本环境未复跑 10 并发。
- **修复**：并发调查缩短锁顺序 / 死锁重试；租约丢失应 fail-closed 单事件，不应打翻整组冒烟。标 P1：阻塞发布门禁，但是编排竞态而非假闭环。

### ID-BLK-005 | 已排除 | required 无 CONFIRMED 写回即 CLOSED

- **位置**：[`workflow.py:931-1084`](backend/app/models/workflow.py)
- **结论**：普通 `transition` 要求报告 + side-effect 收敛 + 终态 `EVENT_STATUS_UPDATE` CONFIRMED。非 mock 拒 simulated 与弱 evidence（227/333）。Mock 宽门按设计。`disposition_is_mock` 默认 **False**（ISSUE-345）。模式判断 **allowlist `mock_xdr`**（ISSUE-344）。

### ID-BLK-006 | 已排除 | Verify `already_submitted` 跳过两阶段

- **位置**：[`verify_agent.py:1012-1043,1647-1654`](backend/app/agents/verify_agent.py)
- **本环**：`verify_agent_ran=true`，`verification_present=true`，走到 CLOSED。

---

## 4. 维度 2 — Bypass / Dead Code / No-op

### ID-BYP-001 | ISSUE-328（同 BLK-001）

预期：高置信已识别主机应隔离。实际：任意遏制即过。Issue #959。

### ID-BYP-002 | Memory / Storyline 不在图节点

- **预期（方案 12 Agent）**：均在调查图。
- **实际**：ISSUE-305 有意。`GRAPH_EXECUTABLE_AGENTS` 现由 P0 节点派生（ISSUE-343/#986 已修死常量）。
- **本环**：`node_trace` 无 memory/storyline。不要把 Memory 塞进图。

### ID-BYP-003 | Trajectory 基础设施失败 | **已修（ISSUE-341）**

[`trajectory.py`](backend/app/api/v1/trajectory.py) SQLAlchemy/OSError → `DependencyUnavailableError` **503**；空轨迹仍 200 + `insufficient_trace`。

### ID-BYP-004 | bootstrap-demo 名称 | **已修（ISSUE-352）**

`bootstrap-demo` / `bootstrap-demo-analysis` = 分析种子；`bootstrap-demo-full-loop` / `demo-full-loop` = CLOSED 金路径。README 已写明。

### ID-BYP-005 | playbook 解析失败跳过 LLM | **已修（ISSUE-340）**

失败 fall through LLM，再规则。本环 Mock `generated_by=llm`（golden），未打中该分支。

### ID-BYP-006 | `block_ip` 封 RFC1918 | **已修（ISSUE-339）**

`_filter_block_ip_entities` 默认只 external。本环仅 `block_ip(198.51.100.44)`。

---

## 5. 维度 3 — 契约漂移与测试假象

### ID-CTR-001 | CI Mock adversarial ≠ Live 研判

- **契约**：`backend-closure-gates` 注释已写 ISSUE-334「Mock 不是 Live」。
- **实现**：job **不设** `LLM_MODE`；golden 含 isolate WKS。本机 Mock full_loop **绿**且隔离 WKS。
- **风险**：对外把 Checks 绿说成自主遏制。仍 OPEN：**ISSUE-350 #994**。
- **建议**：job 展示名写 Mock；Live 对抗 nightly。禁止未修 328 就删 golden isolate。

### ID-CTR-002 | 人类 PASS 与理解 0 并存（319/349 残留）

- **实现**：计分不含 `text_understanding`；349 已旁注。本环 AnalysisOnly：`PASS — coverage GAP; understanding 0/3`。
- **风险**：答辩只截 PASS。旁注已降低风险；若产品要 PASS 变红需另议。

### ID-CTR-003 | DB 隔离仍 gated

- **实现**：`must_response_targets_gated` + 默认不 strict。本环 `disposition_targets_strict_aligned=false`。
- **修复**：与 328 同 PR 解 gated；不要只靠 env 把测试打红当产品修复。

### ID-CTR-004 | artifact pending vs 报告 | **已修（ISSUE-342）**

本环 `response_plan_actions[].status=success`，并导出 `response_plan_generated_by` / `strategy_summary`。

### ID-CTR-005 | OpenAPI | 本 run `contract-drift` **success**

### ID-CTR-006 | DNS 输入链 | **已修（ISSUE-338）**

本环 source_projection 含 `storage-sync-cdn.example`；无 `query_dns` skip-failed。

### ID-CTR-007 | skipif 绑 PG/Redis；零 xfail

- **实现**：`backend/` **无** `@pytest.mark.xfail`。无 Docker 的切片会 skip 可靠性用例。本次有本机 PG/Redis，对抗已跑。

---

## 6. 维度 4 — 安全 / Fail-Open

### ID-SEC-001 | `DEV_AUTH_TOKENS` 生产拒绝

fail-closed（config 启动校验）。非 bug。

### ID-SEC-002 | 分析内容出站 | **未找到泄露路径**

`DispositionCommand` `extra=forbid`；factory 不拷 reason/prompt/report；guard allowlist。

### ID-SEC-003 | `raw_payload` 落库明文 | **SUSPECTED（历史）**

缓解：日志脱敏、GET source-record 不回 raw；迁移 `0036_evidence_raw_data_sanitize`。未做带密码字段的 ingest 抓包，保持 SUSPECTED。

### ID-SEC-004 | `force_local_close` | P2 有意逃生舱

[`state_machine_service.py:533-585`](backend/app/services/state_machine_service.py)：跳过写回门禁，标 `external_unsynced`。API **与** 服务层 admin RBAC（ISSUE-308）。保持 admin-only；可告警，不要绑回写回。

### ID-SEC-005 | Mock 冒充 live 写回 | 本环境已排除

`ALLOW_XDR_WRITEBACK=false`；产物 `certification_label=mock_simulated`。

### ID-SEC-006 | Verify vs CLOSED 档位（live）| P2

Verify 只把 `adapter_acknowledged` 降级（`verify_agent.py:1647-1654`）；CLOSED 非 mock 还拒 `status_queried`。Live 可能 Verify SUCCESS 后关单失败卡在 REPORTING。Mock 本环未打中。

---

## 7. 维度 5 — 可靠性 / 竞态

### ID-REL-001 | system 并发死锁 | 同 BLK-004

### ID-REL-002 | degraded_flags | **部分已修（ISSUE-353）**

bootstrap 走 factory；Redis 自愈已接线。其余 flag 仍可能无 recovery——未在本环动态打满。

### ID-REL-003 | ISSUE-324 调度可观测 | 已合入，本审计未打 SoftTimeLimit

### ID-REL-004 | `disposition_is_mock` 默认 True | **已修（ISSUE-345）→ False**

### ID-REL-005 | `"mock" in mode` 子串 | **已修（ISSUE-344）**

---

## 8. 维度 6 — DX / Demo / 答辩

### ID-DEMO-001 | 官方金路径

README：`make up-demo && make demo-full-loop`。本环境无 Docker，**未跑 compose 金路径**。对抗 full_loop 已覆盖 Agent 图 CLOSED。

### ID-DEMO-002 | main CI 当前非全绿

run `31881813666`（探查时 in_progress）：

| Job | 结论 |
|-----|------|
| backend-closure-gates | success（Mock） |
| contract-drift / frontend-build / regression / docker-build | success |
| backend-lint | **failure**（format 5 files） |
| backend-evaluation | **failure**（detection fail_closed） |
| backend-system-test | **failure**（并发死锁） |
| backend-test | 当时仍 in_progress |

**答辩**：不能声称 main 门禁全绿。

### ID-DEMO-003 | 全链路多为 Mock

本报告两条全链路是 Mock golden。ISSUE-350 仍开。

### ID-DEMO-004 | 双 severity | ISSUE-330 已披露，按设计

### ID-DEMO-005 | 开放热修：ISSUE-328（#959）、ISSUE-350（#994）

### ID-DEMO-006 | FP e2e 选择器 | **已修（ISSUE-336）**

### ID-DEMO-007 | #970 循环导入 | P1 SUSPECTED

[`workflow_graph.py:85`](backend/app/orchestration/workflow_graph.py) import `run_rag_stage`；[`analysis_only_pipeline.py:918-920`](backend/app/services/analysis_only_pipeline.py) 对 `super_agent` **lazy import** 以避免环。本环两条测试均加载成功。保留 #970：其它入口（API 急切 import）仍可能炸。

---

## 9. 发布建议

**需改后发。** 顺序：

1. CI：`ruff format` 五测试文件；查清 detection `fail_closed`；处理并发死锁或隔离 flaky。
2. **ISSUE-328**：实体类 containment merge + DATA_EXFIL 规则 isolate + 解 DB gated；同 PR 改 ISSUE-198 noop 单测。
3. 答辩只用 full_loop + 处置覆盖；Mock closure-gates 绿只说管道。
4. 有 `.env.live` 的机器再跑 **同一对** adversarial（AnalysisOnly + full_loop）Live glm，才能关闭「研判质量」缺口。

### 明确不要做的

- 不要把本次 Mock 5.5s/11.9s PASS 当成 glm 研判证明。
- 不要只把 `isolate_host` 加进 DATA_EXFIL 规则就关 328。
- 不要把 isolate 塞进 `validate_closed_gate`。
- 不要把 detection `required_gate` 改 false。
- 不要恢复 CoT；不要翻 `writeback_required`。
- 不要把 Mock `readback_verified` 字段对外说成已验证（认证标签已是 `mock_simulated`）。

---

## 10. 证据索引（无密钥）

| 项 | 位置 |
|----|------|
| AnalysisOnly 日志 | `/tmp/adversarial_analysis_only.log`；产物 `backend/tests/adversarial/artifacts/latest_audit.json` |
| full_loop 日志 | `/tmp/adversarial_full_loop.log`；产物 `…/latest_full_loop_audit.json` |
| CI | `https://github.com/munos1233/shadowtrace-/actions/runs/31881813666` |
| 328 | https://github.com/munos1233/shadowtrace-/issues/959 |
| 350 | https://github.com/munos1233/shadowtrace-/issues/994 |

---

## 11. 对照 ISSUE-335–353（本 commit 已合入的不要重报未修）

| Issue | 本轮裁定 |
|-------|----------|
| 335 E501 | 产品行已拆；**新**红是测试 `ruff format` |
| 336 backend-test 夹具 | 代码侧已补 FakeEventService / FP 标题 / intent_id |
| 337 evaluation Redis | **已加**；当前红是 detection 评测 fail_closed |
| 338–342 | 本环产物支持已修（域进 projection、artifact success、playbook fall-through、trajectory 503、RFC1918 过滤） |
| 343 死常量 | 已派生自 P0 节点 |
| 344–345 mock 判断/默认 | 已修 |
| 346 MIN_EVIDENCE vs CLOSED | 仍是 312 合同；分数卡已有 `evidence_collection_ok` |
| 347 OutputQuality 默认不挡 | 仍默认 false；本环 triage 0.565 未挡关单 |
| 348 图内 report_quality | 本环写入 `incomplete_placeholder`，不挡 CLOSED |
| 349 人类旁注 | **已见** coverage/understanding 旁注 |
| 350 Mock≠Live | **仍 OPEN** |
| 351 认证语言 | **已见** `mock_simulated` |
| 352 bootstrap 命名 | **已修** |
| 353 degraded factory | **已合入** |
