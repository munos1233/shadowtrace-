{
  "$defs": {
    "DetectionRuleRuntimeState": {
      "description": "Executable package state — not governance approval or production promotion.",
      "enum": [
        "draft",
        "validated",
        "shadow_active",
        "production_active",
        "disabled"
      ],
      "title": "DetectionRuleRuntimeState",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "properties": {
    "source_tenant_id": {
      "maxLength": 128,
      "minLength": 1,
      "title": "Source Tenant Id",
      "type": "string"
    },
    "runtime_state": {
      "anyOf": [
        {
          "$ref": "#/$defs/DetectionRuleRuntimeState"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "page": {
      "default": 1,
      "minimum": 1,
      "title": "Page",
      "type": "integer"
    },
    "page_size": {
      "default": 50,
      "maximum": 200,
      "minimum": 1,
      "title": "Page Size",
      "type": "integer"
    }
  },
  "required": [
    "source_tenant_id"
  ],
  "title": "DetectionRulePackageQuery",
  "type": "object"
}
